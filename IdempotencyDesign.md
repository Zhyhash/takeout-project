# 幂等与重复请求设计

## 1. 文档目的与结论

本文记录当前项目对重复请求的判断、已经生效的保护、Redis 实验方案和仍需接受的业务边界。判断依据是当前 Controller、Service、Mapper、实体、`MysqlApiIntegrationTest.recreateSchema()` 以及并发集成测试。

当前结论：

- `POST /order` 已通过客户端 `requestId`、数据库唯一键和本地事务实现结果可重放的幂等；
- Redis 状态机已完成实验验证，但未接入 `OrderController`，不能描述为当前 API 的实际执行链路；
- 订单取消、支付、确认通过条件状态更新保证同一转换只执行一次；
- 购物车数量指令、部分删除类接口只保证结果安全，或明确接受重复指令语义；
- 真实支付等外部副作用尚未接入，因此不在现有数据库状态机的保障范围内。

## 2. 重复请求的定义

重复请求是同一个业务主体为了完成同一次业务意图，在合理时间内提交了两次或更多次请求。它不等于“HTTP 方法、URL 和参数相同”。

- 用户连续点击两次购物车 `+1`，可以被定义为两个独立的数量指令；
- 用户提交订单后因响应超时而重试，通常仍是同一次下单意图；
- 同一用户主动创建两张内容相同的订单，应使用不同 `requestId`，表示两次业务意图。

本文区分两种目标：

- **结果安全**：重复执行不会产生第二份关键副作用，但后续调用可以返回业务错误；
- **结果可重放**：重复执行不会产生第二份副作用，而且调用方能够得到第一次执行形成的业务结果。

订单创建要求结果可重放；低风险状态设置或删除通常只要求结果安全。

## 3. 当前生效的订单创建幂等

### 3.1 幂等键

`CreateOrderDTO.requestId` 是客户端为一次下单意图生成的标识：

- `@NotBlank`，最长 64 个字符；
- 作用域是当前登录用户；
- 数据库字段是 `orders.request_id varchar(64) NOT NULL`；
- 唯一约束是 `uk_orders_user_request_id(user_id, request_id)`。

因此同一用户重复使用同一个 `requestId` 会命中同一订单，不同用户可以使用相同值。客户端只有在明确发起一笔新订单时才应生成新的 `requestId`；超时重试必须复用原值。

### 3.2 执行链路

`OrderController` 当前直接调用 `OrderService.createOrder()`：

1. 按 `(userId, requestId)` 查询已存在订单；命中后直接构造并返回该订单结果。
2. 未命中时读取可用购物车、商品和营业中的商家，并计算当前金额。
3. 调用独立的 `OrderTransactionExecutor`，由其开启事务。
4. 事务先写 `orders`。唯一键是相同请求并发时的最终裁决点。
5. 新订单继续删除本次读取的购物车行，并校验实际删除行数与预期一致。
6. 对每个商品执行带 `stock >= quantity` 条件的原子扣减，再写入订单明细。
7. 任一步失败，订单、购物车删除、库存扣减和明细写入全部回滚。

### 3.3 并发语义

相同 `(user_id, request_id)` 的两个事务并发插入时，InnoDB 唯一键负责等待和裁决：

- 首事务提交：第二次插入触发重复键，服务查询并返回首事务已经提交的订单；
- 首事务回滚：唯一键不留下记录，第二个事务可以继续插入并完成订单；
- 顺序重试：请求入口直接查询并返回已有订单，不再读取或消费新的购物车内容。

不同 `requestId` 并发消费同一批购物车时，不应被视为同一次意图。当前通过购物车删除行数校验保证最多一个事务完成；失败的事务整体回滚并返回业务错误。

### 3.4 保证与限制

当前方案保证：

- 同一用户、同一 `requestId` 最多形成一张订单；
- 库存最多按该订单扣减一次；
- 订单明细最多写入一组；
- 成功响应丢失后，重试能获得同一个 `orderId`；
- 首次失败并回滚后，同一个 `requestId` 可以再次尝试。

当前返回的是数据库中订单的当前数据，不保存第一次 HTTP 响应的序列化快照。若订单状态已从待支付变为已支付，重复查询应以最新持久化状态为准。

## 4. Redis 订单幂等实验

### 4.1 状态模型

`RedisOrderCreationExperiment` 使用以下 key 和状态：

```text
key: idempotency:order:create:{userId}:{requestId}

MISSING
  -> SET NX PROCESSING，TTL 10 秒
  -> 数据库订单成功后写 SUCCEEDED:{orderId}，TTL 10 分钟
```

- `SUCCEEDED`：按缓存的订单 ID 和当前用户查询订单并返回；
- `PROCESSING`：返回“订单正在创建中”；
- 订单事务失败：清除 key，允许重试；
- Redis 成功状态回写失败：数据库订单仍已提交，清理 key 后的重试由数据库幂等恢复，并再次写入 `SUCCEEDED`；
- 请求入口读取 Redis 抛出 `DataAccessException`：直接降级到 `OrderService` 的数据库方案。

### 4.2 当前定位

Redis 方案只经过 `RedisServiceIntegrationTest` 直接调用验证，尚未接入 `OrderController`。它的目标是让同一请求在到达数据库事务前快速合并或拒绝，减少数据库压力；它不是正确性的最终来源。

即使以后接入，也必须保留数据库唯一键，因为：

- Redis 可能不可用或状态回写失败；
- `PROCESSING` 有 TTL，长事务可能越过租约；
- 缓存数据可能被淘汰或人工清理；
- 数据库才持有订单和副作用的最终状态。

接入前还需要统一 `get`、`SET NX`、写 `SUCCEEDED` 和删除 key 各阶段的异常降级语义，并决定 `PROCESSING` 是返回错误、轮询还是短等待。

## 5. 其他接口的当前判断

### 5.1 已有并发或结果安全保护

| 接口/操作 | 当前保护 | 幂等结论 |
|---|---|---|
| `PATCH /order/{id}/cancel` | `WAIT_PAY -> CANCELLED` 条件更新，随后在同一事务归还库存 | 重复或并发取消只有一次成功，库存只归还一次；不重放首次响应 |
| `PATCH /order/{id}/pay` | `WAIT_PAY -> PAYING -> PAID` 条件更新 | 当前模拟支付下结果安全；不代表真实渠道扣款幂等 |
| `PATCH /order/{id}/confirm` | `PAID -> FINISHED` 条件更新 | 重复或并发确认只有一次成功 |
| `POST /category` | 服务层提前查重，数据库唯一键 `(merchant_id, category_name)` 最终兜底 | 并发重复分类被数据库阻止；重复调用不重放首次响应 |
| 商品实体更新 | `product.version` 与 MyBatis-Plus 乐观锁 | Mapper 层已验证过期版本更新失败；当前商品上下架 Service 的版本写法仍导致 API 集成测试失败，不能视为已完成 |
| 商家资料或营业状态更新 | `merchant.version` 乐观锁 | 防止旧版本静默覆盖新版本 |
| `DELETE /cart/items` | 删除条件包含当前用户，删除行数必须匹配请求数量 | 首次成功后重复删除会报错，最终状态安全 |
| `DELETE /cart/items/all` | 按当前用户清空 | 重复执行后仍为空 |
| 分类删除与商品创建 | 双方对目标分类行 `FOR UPDATE`，并有分类外键 | 防止并发产生孤儿商品 |

### 5.2 接受重复指令语义

| 接口 | 当前语义 | 接受条件 |
|---|---|---|
| `POST /cart/items` | 每次调用代表一次 `quantity + 1` | 加购不扣库存、不产生支付义务，用户下单前能检查数量 |
| `PATCH /cart/items` | 每次 `+1/-1` 是独立指令，`version` 只防止并发覆盖 | 客户端不得把自动重试误当作同一数量意图；需要重放时应改为绝对数量或增加操作 ID |
| `POST /user/register`、`POST /merchant/register` | 数据库用户名和手机号唯一；重复注册返回已存在或唯一键错误 | 注册暂不附带发券、计费等不可重复外部权益 |
| 分类、购物车条目等删除 | 关注最终资源状态，不要求重复请求都返回第一次的成功响应 | 调用方能够在错误后刷新最新状态 |

### 5.3 无需专门处理

- 所有 `GET` 查询；
- `POST /user/login`、`POST /merchant/login`：JWT 无服务端会话写入副作用；
- `PUT /merchant/info`：按请求内容覆盖资料，但并发覆盖由商家版本保护；
- `PATCH /merchant/info?status=...`：设置绝对状态，不是相对切换；
- 购物车清空和显式同状态返回的操作。

当前商品上下架 API 是已知例外：同状态分支可以直接返回，但需要改变状态时，上架路径的显式版本赋值会与乐观锁插件共同处理版本而更新 0 行；下架路径当前还设置了错误的 `ON_SALE` 目标状态。因此本文不把上下架接口列为已经验证的幂等实现。

## 6. 仍需重新评估的场景

### 6.1 真实支付与事务外副作用

当前 `paying()` 只是本地返回成功。接入支付渠道后，需要以支付单号或渠道交易号实现：

- 发起支付幂等；
- 支付回调去重；
- 主动查询与回调并发协调；
- 退款幂等；
- 数据库订单与渠道账单对账。

同理，取消订单未来若加入退款、消息、优惠券或第三方库存，数据库状态前置条件不足以单独保证外部系统只执行一次。

### 6.2 单商家购物车约束

加入商品前的“是否已有其他商家”检查属于先查后插。两个不同商家的商品并发加入空购物车时，仍可能都成功。当前列表和下单会识别多商家并禁止购买，因此不会形成跨商家订单，但该结构不满足“数据库层绝不出现多商家购物车”。若要彻底解决，应引入每个用户唯一的购物车主表，并在主表固定 `merchant_id`。

### 6.3 新的自动重试链路

如果网关、消息队列、离线客户端或后台任务开始自动重试当前按“每次调用都是独立指令”处理的接口，需要重新划分业务意图并引入操作 ID，不能直接沿用现有接受结论。

## 7. 最终原则

1. 高价值创建操作用业务请求键识别同一次意图。
2. 唯一键或条件更新必须在数据库层做最终并发裁决。
3. 一个业务动作的数据库副作用放在同一事务内，并对每个关键写入校验影响行数。
4. Redis、进程锁和前端防抖只能优化体验与负载，不能替代数据库最终约束。
5. 涉及支付、消息或第三方系统时，为每种外部副作用单独设计幂等与对账，而不是把本地订单状态当作完整答案。
