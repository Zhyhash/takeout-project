# 幂等风险判断

## 1. 文档目的

本文不讨论幂等键、分布式锁、缓存、数据库表等实现方案，只回答四个问题：

1. 什么情况属于重复请求；
2. 当前项目哪些接口存在幂等风险；
3. 哪些风险值得投入解决；
4. 哪些风险可以在明确的业务约束下接受。

评估基于当前仓库中的 Controller、Service、Mapper、实体和集成测试数据库结构。这里优先判断业务副作用是否安全，不要求所有重复调用都返回完全相同的 HTTP 响应。

## 2. 什么是重复请求

重复请求不是简单的“HTTP 方法、URL 和参数相同”，而是：

> 同一个业务主体，为了完成同一次业务意图，在合理时间范围内向系统提交了两次或更多次请求。

常见来源包括双击按钮、页面重复提交、客户端或网关自动重试、请求超时后人工重试，以及两个并发请求同时到达。

判断时需要区分“请求相同”和“业务意图相同”：

- 用户连续点击两次“购物车数量 +1”，请求内容相同，但可能代表两次真实的加购意图；
- 用户提交订单后因响应超时再次提交，虽然两次调用发生在不同时间，但通常仍是同一次下单意图；
- 同一个用户先后提交两个内容完全相同的订单，如果用户明确要购买两次，则不是重复请求。

本项目把以下任一情况视为存在幂等风险：

- 同一次业务意图被再次执行后，又创建了一份业务数据；
- 金额、库存、数量等被重复增加或扣减；
- 第一次已经成功，但调用方无法确认结果，重试只能得到无关错误；
- 并发重复执行可能绕过“先查后写”的校验。

需要同时区分两种安全程度：

- **结果安全**：重复执行不会产生第二份副作用，但第二次可以返回“已处理”或业务错误；
- **结果可重放**：重复执行不仅不产生第二份副作用，还能让调用方确认第一次的业务结果。

对于订单、真实支付等高价值操作，应关注结果可重放。对于删除、上下架等低风险操作，只要最终业务状态正确，第二次返回业务错误通常可以接受。

## 3. 判断标准

风险是否值得解决，按以下四项综合判断：

1. **影响大小**：是否涉及订单、资金、库存，或会污染后续业务数据；
2. **发生概率**：接口是否面向普通用户、是否高频、是否容易超时或被自动重试；
3. **现有保护**：是否已有唯一约束、状态前置条件、事务或乐观锁；
4. **恢复成本**：用户能否自行看见并纠正，还是需要人工对账和数据修复。

本文使用三类结论：

- **必须解决**：不应只依赖前端防重或用户重试习惯；
- **条件性必须解决**：当前实现尚无实际高危副作用，但业务升级前必须解决；
- **接受业务约束**：当前阶段可以不增加专门的幂等能力，但接受条件必须持续成立。

## 4. 当前接口风险盘点

### 4.1 值得解决的风险

| 优先级 | 接口 | 当前重复执行表现 | 主要风险 | 判断 |
| --- | --- | --- | --- | --- |
| P0 | `POST /order` | 创建订单会删除购物车、扣减库存并写入订单和明细。事务和“删除购物车行数校验”通常能让同一购物车的并发请求只有一个成功，但首次成功后重试只会得到“购物车为空”，无法确认原订单结果 | 订单是高价值结果；当前保护更接近“最多成功一次”，不是对同一次下单意图的稳定识别。响应丢失会给用户留下结果不确定性；购物车内容发生变化后再次重试还可能消费新的购物车快照 | 未来可以添加requestId解决，当前业务不必要引入额外的机制，目前购物车->订单机制已经可以降低重复提交造成的风险，如需简单幂等，可以加入时间窗口+刷新机制 |
| 上线门槛 | `PATCH /order/{id}/pay` | 当前 `paying()` 只是本地模拟；订单状态前置条件和 `version` 乐观锁可阻止本地状态被重复推进 | 当前没有真实扣款，所以实际资金风险尚未出现。一旦接入支付渠道，网络超时、支付回调和主动查询都可能重复触发，同一支付意图不能依赖本地订单状态来代表渠道侧只扣款一次 | **接入真实支付前必须解决** |

`POST /order` 的现有事务、库存条件更新和购物车删除校验应继续视为并发一致性保护，但不能据此认定接口已经具备完整幂等语义。该接口达到可接受状态时，业务上至少应满足：同一次下单意图最多形成一张订单、库存最多扣减一次，并且重试方能够确认第一次形成的订单结果。

商品创建的重复判断不能简单等同于“商品名称相同”。当前恢复商品的逻辑已经隐含“同一商家不希望存在同名在售商品”的业务倾向，但系统仍可能允许商家有意创建同名的不同规格商品。因此需要解决的是“同一次创建动作被执行多次”，不是在本文中直接认定商品名称就是业务唯一标识。

### 4.2 存在风险，但当前接受业务约束

| 接口 | 当前行为与剩余风险 | 当前判断 |
| --- | --- | --- |
| `POST /cart/items` | SQL 使用 `quantity = quantity + 1`；相同请求每执行一次都会再增加一件商品。这既可能是重复提交，也可能是用户真实连续加购 | 接受“每次调用代表一次 +1 指令”的业务语义 |
| `PATCH /cart/items` | 请求使用相对变化量 `+1/-1`；顺序重复会累计变化，并发更新由 `version` 乐观锁避免静默覆盖，但不能识别两次调用是否属于同一意图 | 与加购相同，按数量指令接受 |
| `POST /category` | 顺序重复会被同商家、同名称的查询拦截；但集成测试 schema 中没有 `(merchant_id, category_name)` 唯一约束，并发的“先查后插”仍可能生成重复分类 | 当前按低频商家后台操作接受；不提升为通用幂等建设项 |
| `POST /user/register`、`POST /merchant/register` | 顺序重复会返回“已存在”；测试 schema 对用户名和手机号设置了唯一索引，可阻止并发创建重复账号，但不会重放第一次注册成功响应 | 接受数据库唯一性保证下的“一个成功、其余报已存在” |
| `DELETE /cart/items` | 第一次删除成功后，完全相同的重试会因删除行数不匹配而报错；最终状态仍是已删除 | 接受结果安全，不要求响应可重放 |
| `DELETE /category/{id}`、`POST /merchant/restore/{id}` | 第一次成功后再次调用会因资源不存在或状态不符而报错；不会产生第二份有效副作用 | 接受结果安全，不要求响应可重放 |
| `PATCH /order/{id}/cancel` | 取消会归还库存，属于敏感副作用；当前只允许 `WAIT_PAY -> CANCELLED`，订单带乐观锁且整个操作在事务中。顺序或并发重复不会再次归还库存，但重复请求会收到状态不符错误 | 接受现有状态机约束；若未来取消包含退款、发券等外部副作用，需重新评估 |
| `PATCH /order/{id}/confirm` | 只允许 `PAID -> FINISHED`，订单乐观锁可阻止并发重复推进；第二次请求返回状态不符 | 当前接受 |

以上“接受”不是认定接口天然无风险，而是基于当前业务价值、调用频率和可恢复性，认为专门处理重复意图的收益暂时低于成本。

### 4.3 无需专门处理的接口

以下接口重复执行后的核心业务状态相同，或者本身没有持久化业务副作用：

- 所有 `GET` 查询接口；
- `POST /user/login`、`POST /merchant/login`：会生成登录令牌，但当前采用无服务端会话副作用的 JWT 模式，多次登录属于会话策略问题，不属于本次幂等风险；
- `PUT /merchant/info`：按请求内容覆盖商家信息；
- `PATCH /merchant/info?status=...`：设置绝对营业状态，不是相对切换；
- `PATCH /category/products/{id}/on-shelf`：已经上架时直接返回当前商品；
- `PATCH /category/products/{id}/off-shelf`：已经下架时直接返回当前商品；
- `DELETE /cart/items/all`：重复清空后仍为空。

HTTP 方法本身不是判断依据。例如 `POST /login` 没有本次关注的业务副作用，而 `PATCH /cart/items` 因使用相对增减语义仍然存在重复累计风险。

## 5. 接受风险所依赖的业务约束

只有在以下约束成立时，第 4.2 节的风险才可以继续接受：

### 5.1 购物车数量操作

- 每次调用都被定义为一次独立的数量变更指令；
- 购物车变更不预占或扣减库存，不直接产生支付义务；
- 用户下单前能够看到最终数量和金额，并能自行修改。

如果未来出现一键购买、加购即锁库存、后台自动重试或弱网络离线重放，这一接受结论失效。

### 5.2 分类创建

- 分类只由低频商家后台操作产生；
- 默认不存在批量导入、多终端同时操作或系统自动重试；
- 重复分类可被商家识别和删除，删除时商品会迁移到默认分类。

如果支持多店员协作、批量导入或外部系统同步，应重新将该接口列入待解决范围。

### 5.3 注册

- 生产数据库必须与测试 schema 一致，对用户和商家的用户名、手机号保留唯一约束；
- 重复注册返回“已存在”可以被客户端和用户接受；
- 注册成功暂不附带不可重复的外部权益、发券、计费或通知动作。

仓库中没有生产数据库迁移文件，因此“生产库确实存在唯一约束”是本判断的前置假设，而不是已经由仓库完全证明的事实。

### 5.4 删除、恢复和订单状态切换

- 业务只要求最终状态正确，不要求每次重试都返回第一次的成功响应；
- 调用方能够把“资源不存在”“状态不符”理解为刷新状态的信号；
- 当前操作的副作用全部受同一数据库事务和状态条件约束。

一旦操作加入支付、退款、消息投递、第三方库存或营销权益等事务外副作用，现有接受结论失效。

### 5.5 当前模拟支付

- `paying()` 仅用于本地流程演示，不代表真实资金扣减；
- 项目不得把当前接口的本地状态保护直接视为真实支付幂等已经完成。

## 6. 最终结论

当前阶段的处理顺序为：

1. **优先解决 `POST /order`**：它已经涉及订单、库存和用户结果不确定性，是当前唯一需要立即按高价值重复请求对待的接口；
2. **随后处理 `POST /category/products`**：避免重复创建的商品进入购物车和订单后扩大修复成本；
3. **真实支付接入前，把 `PATCH /order/{id}/pay` 作为上线阻断项重新评审**；
4. 购物车相对增减、分类创建、重复注册、删除和现有订单状态切换暂时接受业务约束，不进行一刀切的统一幂等建设；
5. GET、绝对状态设置、清空和显式同状态返回的接口无需专门处理。

本判断的核心原则是：幂等投入跟随业务损失，而不是跟随 HTTP 方法。高价值且结果难恢复的操作要求系统识别同一次业务意图；低价值、可见、可撤销的操作可以在约束明确的前提下接受重复请求风险。
