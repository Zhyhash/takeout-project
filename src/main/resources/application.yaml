spring:
  application:
    name: tokeout
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver # 数据库驱动
      url: jdbc:mysql://localhost:3306/mp_demo?serverTimezone=GMT%2B8 # 数据库连接地址
      username: root  # 数据库账号
      password: root # 数据库密码
  mybatis-plus:
      mapper-locations: classpath:mapper/*.xml