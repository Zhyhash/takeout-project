# 当前实际业务状态流转与最小闭环

## 一、文档目标

本文以当前 `OrderService`、`MerchantService`、`DeliveryTaskService`、状态枚举和 Mapper 实现为准，记录用户、商家和骑手三方已经落地的最小业务闭环。

需要解决的问题包括：

- 三类主体分别可以执行什么动作；
- 每个动作允许从什么状态开始；
- 动作成功后改变什么业务事实；
- 哪些操作必须检查主体归属；
- 哪些操作存在重复请求或并发竞争；
- 骑手和配送任务是否应成为独立实体；
- 订单状态与配送任务状态如何保持一致；
- 哪些真实业务暂时不纳入 MVP。

------

# 二、核心设计结论

加入骑手后，系统不再只是简单的订单状态管理，而是形成三个相互关联、职责不同的业务实体：

```text
Order：用户购买与订单履约事实
Rider：骑手身份与账号信息
DeliveryTask：具体配送责任与配送过程
```

最终关系为：

```text
User ──创建──> Order <──归属── Merchant
                  │
                  │ 1 : 1
                  ▼
             DeliveryTask
                  │
                  ▼
                Rider
```

当前 MVP 中，一张订单最多对应一张配送任务。

配送任务单向关联订单：

```text
DeliveryTask.orderId → Order.id
```

Order 暂时不必保存 `deliveryTaskId`，避免订单实体反向依赖配送实现。

------

# 三、为什么拆分 DeliveryTask

最小实现确实可以直接在订单表中增加：

```text
riderId
deliveryAcceptTime
deliveryTime
```

但当前配送行为已经具备独立的：

- 责任主体；
- 生命周期；
- 状态变化；
- 并发竞争；
- 时间记录；
- 未来配送费和骑手结算；
- 配送异常和重新派单可能。

因此，配送已经不再只是订单的几个附属字段，而是一个独立业务事实。

拆分依据不是单纯的“字段太多”或“方便扩展”，而是：

> 配送任务拥有独立生命周期和独立责任归属，不应全部塞入订单实体。

------

# 四、订单主状态机

订单负责表达用户购买和整体履约进度。

```text
WAIT_PAY
    │ 用户支付
    ▼
PAYING
    │ 模拟支付成功
    ▼
PAID
    │ 商家接单
    ▼
PREPARING
    │ 商家制作完成
    ▼
READY
    │ 骑手接取任务并开始配送
    ▼
DELIVERING
    │ 骑手确认送达
    ▼
DELIVERED
    │ 用户确认收货
    ▼
FINISHED
```

已有取消分支：

```text
WAIT_PAY → CANCELLED
```

`PAYING` 是 `payOrder` 事务中的中间状态；当前模拟支付固定成功，随后立即进入 `PAID`。如果支付过程抛出异常，事务会回滚。

支付后商家拒单、退款等异常分支暂未实现。

------

# 五、订单状态业务定义

## 1. WAIT_PAY

### 业务事实

用户已经提交订单，订单已经创建，等待用户完成支付。当前代码在创建订单时已经扣减库存。

### 当前代码约束

```text
payTime == null
仅 WAIT_PAY 可以取消或发起支付
```

------

## 2. PAYING

### 业务事实

`payOrder` 先通过条件更新把订单从 `WAIT_PAY` 改为 `PAYING`，再调用当前的模拟支付逻辑。

### 当前代码约束

```text
payTime == null
模拟支付成功后继续进入 PAID
支付失败抛异常，整个事务回滚
```

------

## 3. PAID

### 业务事实

用户已经完成支付，订单正式生效，等待所属商家接单。

### 当前代码约束

```text
payTime != null
商家只允许从 PAID 接单
```

------

## 4. PREPARING

### 业务事实

商家已经接单并开始制作餐品。当前 `Order` 实体没有保存商家接单时间。

### 当前代码约束

```text
order.status == PREPARING
尚未创建 DeliveryTask
```

------

## 5. READY

### 业务事实

商家已经完成制作，订单可以被骑手接取；同一事务内会创建配送任务。当前 `Order` 实体没有保存制作完成时间。

### 当前代码约束

```text
存在 status == WAIT_ASSIGN 的 DeliveryTask
DeliveryTask.riderId == null
```

`READY` 可以持续存在，并不保证立即有骑手接单。

------

## 6. DELIVERING

### 业务事实

骑手已经抢到配送任务并开始配送。当前 MVP 将“骑手接单”“到店取餐”和“开始配送”合并为同一个动作。

### 当前代码约束

```text
DeliveryTask.riderId != null
DeliveryTask.status == DELIVERING
DeliveryTask.acceptedTime != null
```

------

## 7. DELIVERED

### 业务事实

骑手已经确认送达，但用户尚未确认收货，因此订单还没有最终完成。

### 当前代码约束

```text
DeliveryTask.status == COMPLETED
DeliveryTask.deliveredTime != null
finishTime == null
```

------

## 8. FINISHED

### 业务事实

用户已经确认收到餐品，订单整体履约完成。

### 当前代码约束

```text
finishTime != null
DeliveryTask.status == COMPLETED
```

------

## 9. CANCELLED

### 业务事实

当前代码仅支持用户取消 `WAIT_PAY` 订单。取消成功后会按照订单明细归还商品库存。

### 当前代码约束

```text
仅 WAIT_PAY → CANCELLED
重复取消会返回业务异常
当前 Order 没有保存取消原因字段
```

------

# 六、配送任务状态机

DeliveryTask 负责表达配送责任和配送过程。

当前 MVP 状态为：

```text
WAIT_ASSIGN
      │ 骑手接取任务
      ▼
DELIVERING
      │ 骑手确认送达
      ▼
COMPLETED
```

## WAIT_ASSIGN

配送任务已经创建，但尚未绑定骑手。

```text
riderId == null
acceptedTime == null
```

## DELIVERING

任务已经绑定骑手，骑手正在执行配送。

```text
riderId != null
acceptedTime != null
deliveredTime == null
```

## COMPLETED

骑手已经将餐品送达并结束自己的配送责任。

```text
riderId != null
deliveredTime != null
```

配送任务完成并不直接等于订单完成。

```text
DeliveryTask.COMPLETED
    ≠
Order.FINISHED
```

骑手完成配送后，订单进入 `DELIVERED` 并等待用户确认收货；用户确认后，订单才进入 `FINISHED`。

------

# 七、配送任务创建时机

当前 MVP 选择：

> 商家制作完成时创建 DeliveryTask。

即商家执行制作完成操作时，在同一个事务中完成：

```text
1. Order：PREPARING → READY
2. 创建 DeliveryTask
3. DeliveryTask.status = WAIT_ASSIGN
```

不在支付后立即创建配送任务，原因是当前项目不需要提前预测运力或提前调度。

大型平台可能在支付后提前生成配送需求，用于预测、派单和运力调度；但那属于调度效率优化，不是当前 MVP 闭环成立的必要条件。

------

# 八、动作业务契约

| 动作         | 执行主体       | 允许旧状态                                     | 主体归属检查                       | 成功结果                                                           | 当前重复请求语义                         |
| ------------ | -------------- | ---------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------ | ---------------------------------------- |
| 用户支付     | 下单用户本人   | 订单为 `WAIT_PAY`                              | `order.userId == 当前用户`         | 先进入 `PAYING`，成功后写 `payTime` 并进入 `PAID`                  | 已是 `PAID` 时返回业务异常               |
| 用户取消     | 下单用户本人   | 订单为 `WAIT_PAY`                              | `order.userId == 当前用户`         | 订单进入 `CANCELLED`，归还订单商品库存                             | 已取消时返回业务异常                     |
| 商家接单     | 订单所属商家   | 订单为 `PAID`                                  | `order.merchantId == 当前商家`     | 订单进入 `PREPARING`                                               | 已是 `PREPARING` 时直接返回              |
| 商家制作完成 | 订单所属商家   | 订单为 `PREPARING`                             | `order.merchantId == 当前商家`     | 订单进入 `READY`，创建 `WAIT_ASSIGN` 配送任务                      | 已是 `READY` 时直接返回                  |
| 骑手接取任务 | 当前骑手       | 任务为 `WAIT_ASSIGN`，订单为 `READY`           | 任务尚未绑定其他骑手               | 绑定 `riderId`、写 `acceptedTime`，任务和订单进入 `DELIVERING`     | 已由当前骑手接取时直接返回               |
| 骑手确认送达 | 当前任务骑手   | 任务和订单都为 `DELIVERING`                    | `task.riderId == 当前骑手`         | 写 `deliveredTime`，任务进入 `COMPLETED`，订单进入 `DELIVERED`     | 任务和订单已经到达目标状态时直接返回     |
| 用户确认收货 | 下单用户本人   | 订单为 `DELIVERED`                             | `order.userId == 当前用户`         | 写 `finishTime`，订单进入 `FINISHED`                               | 已是 `FINISHED` 时返回业务异常           |

------

# 九、强一致性与事务边界

当前项目是单体 Spring Boot 应用，Order 与 DeliveryTask 使用同一个数据库，因此不需要引入消息队列或最终一致性机制。

两个实体需要在关键动作中进行强一致更新。

## 1. 商家制作完成

同一事务内：

```text
Order：
PREPARING → READY

DeliveryTask：
不存在 → WAIT_ASSIGN
```

不能出现：

```text
Order 已经 READY
但 DeliveryTask 创建失败
```

否则订单已经可以配送，却不存在可供骑手接取的任务。

------

## 2. 骑手接取任务

同一事务内：

```text
DeliveryTask：
WAIT_ASSIGN → DELIVERING
绑定 riderId
写 acceptedTime

Order：
READY → DELIVERING
```

不能出现：

```text
配送任务已绑定骑手，但订单仍是 READY
```

也不能出现：

```text
订单已经 DELIVERING，但配送任务没有骑手
```

------

## 3. 骑手确认送达

同一事务内更新 DeliveryTask 和 Order：

```text
DeliveryTask：
DELIVERING → COMPLETED
写 deliveredTime

Order：
DELIVERING → DELIVERED
```

不能出现任务已经完成但订单仍在配送，或订单已经送达但任务尚未完成。任一步骤失败都会触发事务回滚。

------

## 4. 用户确认收货

检查：

```text
Order.userId == 当前用户
Order.status == DELIVERED
```

当前 `checkedOrder` 依赖骑手确认送达事务已经把订单推进到 `DELIVERED`，不会再次直接查询 DeliveryTask。

成功后：

```text
Order：
DELIVERED → FINISHED
写 finishTime
```

------

# 十、并发与幂等设计

除创建订单外，当前动作大多属于已有实体上的状态迁移，因此可以依靠：

```text
旧状态条件
+
主体归属
+
条件更新
```

当前代码用受影响行数判断状态迁移是否成功，从而实现并发保护。幂等策略并不完全统一：商家接单、制作完成和骑手操作支持目标状态幂等；支付、取消和用户确认收货的重复请求会返回业务异常。只有创建订单使用 `requestId`。

## 普通状态迁移

例如商家接单：

```sql
UPDATE orders
SET status = PREPARING
WHERE id = ?
  AND merchant_id = ?
  AND status = PAID;
```

只有一条请求能够完成状态迁移。

------

## 骑手抢单

骑手接取任务是最高风险并发点。

配送任务更新条件为：

```text
task.status == WAIT_ASSIGN
task.riderId == null
```

任务更新成功后，再通过条件更新执行 `order.status == READY → DELIVERING`。两个更新位于同一事务中，订单更新失败时会回滚任务更新。

成功时必须同时：

```text
绑定 riderId
修改 DeliveryTask 状态
修改 Order 状态
写接单时间
```

并发结果：

- 一名骑手成功；
- 当前成功骑手重试时返回当前任务；
- 其他骑手返回“任务已被其他骑手接取”。

------

# 十一、主体权限检查

角色鉴权不能代替资源归属检查。

## 商家操作

必须同时满足：

```text
当前账号具备商家身份
订单 merchantId 属于当前商家
订单状态允许该动作
```

当前不检查商家是否处于营业状态。

原因是营业状态主要决定是否接受新订单，不应阻止商家继续履行已经支付的存量订单。

------

## 骑手操作

必须满足：

```text
当前账号具备骑手身份
接单时任务尚未绑定其他骑手
后续操作时 task.riderId 属于当前骑手
```

当前身份来自 JWT 中的 `rider` 角色。现有 `DeliveryTaskService` 尚未查询 Rider 表，也没有校验骑手账号 `status`。

------

## 用户操作

必须满足：

```text
当前账号具备用户身份
order.userId 属于当前用户
订单状态允许该动作
```

------

# 十二、当前骑手端接口范围

## 已实现的 DeliveryTask 动作

```text
PATCH /rider/delivery-tasks/{taskId}/claim
    接取配送任务

PATCH /rider/delivery-tasks/{taskId}/complete
    确认已经送达
```

两个接口都要求 JWT 角色为 `rider`。资源归属和状态约束由 `DeliveryTaskService` 再次校验。

------

## 当前尚未实现

```text
骑手注册与登录
查看骑手个人信息
查看可接配送任务列表
查看自己的配送任务列表
查看配送任务详情
骑手专用订单视图
骑手主动取消或转派任务
```

因此，当前 Controller 只闭环“抢单”和“确认送达”两个核心动作，查询和骑手账号管理仍属于后续范围。

------

# 十三、建议的最小数据模型

## Rider

```text
id
name
phone
password
status
createTime
updateTime
isDelete
```

负责表达骑手身份和账号状态。

------

## DeliveryTask

```text
id
orderId
riderId
merchantName
status
createTime
acceptedTime
deliveredTime
updateTime
```

其中：

- `orderId` 必须存在且建议唯一，保证一张订单当前只对应一个配送任务；
- `riderId` 在任务创建时为空，接单成功后写入；
- 当前实体冗余保存 `merchantName`，没有保存 `merchantId`；
- `acceptedTime` 和 `deliveredTime` 分别由抢单和确认送达动作写入。

------

## Order 需要的变化

新增状态：

```text
PAYING
PREPARING
READY
DELIVERING
DELIVERED
```

当前实际保存的业务时间：

```text
payTime
finishTime
```

当前 Order 没有 `acceptTime` 和 `readyTime`。骑手 ID、骑手接单时间和送达时间由 DeliveryTask 保存。

------

# 十四、当前明确不支持的边界

本次 MVP 暂不处理：

- 骑手接单与取餐的独立状态；
- 骑手主动取消任务；
- 骑手接单后转派；
- 无骑手接单后的超时处理；
- 自动派单和配送区域算法；
- 多骑手接力配送；
- 配送失败与重新派单；
- 真实支付和真实退款；
- 商家、骑手收入结算；
- 用户评价系统；
- 消息推送；
- 已支付订单私下更换商品；
- 商家拒单后的完整退款流程。

其中，`READY` 状态可能长期无人接单是已知边界。当前版本只保证流程能够闭环，不保证真实环境下的配送调度效率。

------

# 十五、最终最小闭环

```text
用户创建订单
    ↓
WAIT_PAY

用户支付
    ↓
PAYING
    ↓ 模拟支付成功
PAID

商家接单
    ↓
PREPARING

商家制作完成
    ↓
Order = READY
创建 DeliveryTask = WAIT_ASSIGN

骑手接取任务
    ↓
DeliveryTask = DELIVERING
Order = DELIVERING

骑手确认送达
    ↓
DeliveryTask = COMPLETED
Order = DELIVERED

用户确认收货
    ↓
Order = FINISHED
```

至此，用户、商家和骑手三方都拥有明确动作，订单和配送任务分别拥有清晰生命周期，关键并发点、幂等语义、主体归属和事务边界均已确定。
