# 项目暂缓风险清单

> 项目：Spring Boot 外卖系统 V2  
> 整理日期：2026 年 8 月 11 日
> 范围：仅记录近期讨论中已经明确决定“暂不处理”的问题；已经修复的库存、商品状态、购物车数量、订单状态、分类约束及订单核心幂等问题不再列入。

## 一、风险分类与分级口径

风险等级**仅按问题一旦发生后的业务影响评定，不因发生概率低而下调**；概率、现阶段控制措施和暂缓原因仍在后文保留。一个风险可以带有多个标签，但以下清单为便于归属只标记一个主类型。

| 风险类型 | 适用范围 |
| --- | --- |
| 业务正确性 | 订单、价格、状态、购物车等对用户或商家可见的业务结果不符合规则。 |
| 安全 | 身份认证、授权、敏感数据、注入与越权等风险。当前清单没有独立归入此类的暂缓项；出现后应按影响重新评定。 |
| 一致性与并发 | 多请求、多步骤或多数据源下，数据快照、原子性、幂等性和最终状态不一致。 |
| 可用性与韧性 | 请求失败、死锁、重试、超时和故障恢复能力不足。 |
| 工程架构 | 分层边界、数据库变更管理、组件职责与实现方式带来的长期变更风险。 |
| 质量保障 | 测试覆盖、并发验证和发布前发现问题的能力不足。 |

| 风险等级 | 影响力判定 |
| --- | --- |
| 高 | 可能造成订单重复或错单、用户数据丢失、价格/履约错误、核心数据完整性受损，或需要人工介入才能恢复。 |
| 中 | 会造成单次请求失败、局部状态异常或后续改动高风险，但有事务回滚、校验或重试等措施可限制影响。 |
| 低 | 不直接影响当前核心交易正确性；主要影响后续扩展、维护成本或非核心性能。 |

### 风险索引

| 编号 | 风险项 | 主类型 | 发生后的主要影响 | 风险等级 |
| --- | --- | --- | --- | --- |
| R-01 | 下单期间购物车被修改 | 一致性与并发 | 可能清除用户刚新增但未计入本次订单的购物车数据，造成订单与用户意图不一致。 | 高 |
| R-02 | 同一 `requestId` 携带不同参数 | 业务正确性 | 可能按旧地址、电话等信息返回或履约订单，且调用方无法感知参数冲突。 | 高 |
| R-03 | 下单校验与商品、商家状态发生竞态 | 业务正确性 | 可能以旧价格、已下架商品或已打烊商家的状态继续创建订单。 | 高 |
| R-04 | 生产库幂等唯一索引缺少迁移固化 | 工程架构 | 部署环境遗漏唯一索引时，核心幂等约束失效，可能产生重复订单。 | 高 |
| R-05 | 多商品库存扣减可能死锁 | 可用性与韧性 | 个别订单会被数据库回滚并失败，需要客户端以原 `requestId` 重试；数据本身不会被破坏。 | 中 |
| R-06 | 下单并发验证场景不完整 | 质量保障 | 并发回归可能未在发布前发现，相关问题上线后才会暴露。 | 中 |
| R-07 | 空购物车并发加入不同商家商品 | 一致性与并发 | 可能形成不符合单商家约束的购物车，虽会在结算时被拦截，但会影响用户操作。 | 中 |
| R-08 | 商家状态与商家资料并发更新 | 一致性与并发 | 管理端一次更新可能覆盖另一请求刚提交的字段值。 | 中 |
| R-09 | Redis 幂等状态不进入核心流程 | 工程架构 | 当前数据库唯一约束仍可保证不重复创建订单；主要限制高并发下的削峰与状态协同能力。 | 低 |
| R-10 | Service 层包含持久化 SQL 细节 | 工程架构 | 后续修改更易破坏数据库语义或事务边界，但当前不存在直接的 SQL 注入风险。 | 中 |
| R-11 | `ProductConverter` 隐式决定商品初始状态 | 工程架构 | 业务默认状态藏在转换规则中，后续改动可能意外改变商品创建语义。 | 中 |
| R-12 | 购物车增减接口未提供请求级幂等 | 业务正确性 | 客户端自动重试时，同一增减操作可能重复执行，导致购物车数量偏离用户意图。 | 中 |

## 二、明确暂缓的风险

| 风险项 | 问题与原因 | 暂不处理的原因 |
| --- | --- | --- |
| 下单期间购物车被修改 | `createOrder` 先读取购物车并准备订单数据，之后才在事务中消费购物车。两步之间存在窗口：用户若新增数量或修改条目，后续按条目整行删除可能把刚加入、但未计入本次订单的数据一起清掉。根因是“读取快照”和“删除当前数据”没有用同一版本或同一数量条件绑定。 | 批量使用 `id IN (...) AND version IN (...)` 不能稳定表达每个 `id` 对应哪个 `version`，完整修复又会牵涉逐项版本校验、按数量消费或购物车总表。当前发生窗口较小，因此保留为已知风险。 |
| 同一 `requestId` 携带不同参数 | 当前只用 `userId + requestId` 判断是否为同一次创建订单请求。若客户端错误复用 `requestId`，但修改了地址、电话等 DTO 参数，服务端可能直接返回旧订单，而不会指出参数冲突。根因是没有保存并比较请求指纹。 | 正常客户端按协议应为新的业务意图生成新 `requestId`，该错误概率较低；增加参数快照或哈希会修改表结构和幂等协议，因此本阶段不扩展。 |
| 下单校验与商品、商家状态发生竞态 | `createOrder` 在事务外读取商品价格、上下架状态和商家营业状态，事务内扣库存时没有再次绑定商品状态、价格或商家状态。商品改价、下架或商家打烊与下单并发时，订单可能继续使用事务外读取的旧快照。 | 完整修复需要把结算快照建立、商品及商家校验和库存扣减放进统一事务，并明确价格变化时的业务规则；当前先保留为已知风险。 |
| 生产库幂等唯一索引缺少迁移固化 | 核心幂等依赖 `orders(user_id, request_id)` 唯一索引，但该索引目前只在测试 DDL 和设计文档中体现，主资源目录没有 Flyway/Liquibase 等数据库迁移。若某个部署库漏建该索引，并发下单时“先查再插入”无法阻止重复订单。 | 当前仅为本地练习项目，V2 明确接受手工维护 schema；部署时仍必须人工核验该唯一索引。版本化迁移、统一测试初始化与漂移校验已纳入 V3 工程化建设；一旦增加 CI、共享开发库、预发布或生产环境，本项不再接受，必须先完成。 |
| 多商品库存扣减可能死锁 | 一个订单会逐项更新多个商品库存，但当前没有按统一的商品 ID 顺序执行。两个订单以相反顺序锁定相同商品时，MySQL 可能检测到死锁并回滚其中一个事务。数据通常不会损坏，但请求会失败。 | 数据库能够通过死锁检测保护一致性；统一排序和针对死锁的有限事务重试需要额外设计，当前先接受偶发失败并要求客户端使用原 `requestId` 重试。 |
| 下单并发验证场景不完整 | 现有测试覆盖相同/不同 `requestId` 并发创建，但尚未系统覆盖“下单与购物车修改”“下单与商品改价/下架”“下单与商家打烊”“多用户争抢最后库存”“多商品反向锁定”和各写入阶段强制回滚。少量双线程用例也不能替代重复压力验证。 | 完整并发测试需要可控阻塞点、重复执行和稳定的独立 MySQL 测试环境，当前作为测试债务记录。 |
| 空购物车并发加入不同商家商品 | 两个不同商家的加购请求可能同时看到购物车为空，并分别通过“单商家”校验，最终形成多商家购物车。根因是“先查询、再判断、再写入”无法把用户级单商家约束原子化。 | 发生概率较低，且列表与下单校验能够识别并阻止无效购物车继续结算；彻底解决需要用户/购物车级锁、购物车总表或更强数据库约束，当前收益不足以覆盖改造成本。 |
| 商家状态与商家资料并发更新 | 专门修改营业状态的请求与修改商家资料的请求若同时写入重叠字段，后提交的一方可能把另一方的新值覆盖为旧值。根因是两个写入口的字段所有权没有完全隔离。 | 这两类管理操作并发出现的频率和影响都较低；主体的整实体覆盖问题已修复，剩余边界暂不继续扩大改造范围。 |
| Redis 幂等状态不进入核心流程 | Redis 实验能够快速拦截重复请求并缓存成功结果，但会引入 `PROCESSING` 超时、进程崩溃、键过期、旧请求误删新锁以及数据库成功后 Redis 写回失败等跨系统状态问题。 | 当前数据库唯一约束已经保证最终只创建一张订单，项目数据量也没有重复请求压垮数据库的压力。Redis 继续保留为学习实验，不用额外复杂度替换稳定的数据库主流程。 |
| Service 层包含持久化 SQL 细节 | `ProductService` 通过 `setSql` 直接表达库存扣减，`ProductService` 与 `CategoryService` 通过 `last("FOR UPDATE")` 追加数据库锁语句，导致列名、SQL 算术和数据库方言进入业务层。当前参与拼接的 `quantity` 是经过正数校验的 `Integer`，因此这不是现成的 SQL 注入漏洞，问题在于持久化职责边界不清。 | 本轮不修改 Mapper 代码，现有原子扣库存和加锁语义继续保留。纳入 V3 工程化修复：把库存扣减和分类加锁查询下沉为参数化 Mapper 方法，Service 仅保留业务校验、事务边界和受影响行判断。 |
| `ProductConverter` 隐式决定商品初始状态 | `ProductConverter` 在 DTO 转实体时直接写入 `OFF_SALE` 和 `NOT_DELETED`，使商品创建的业务默认状态隐藏在 MapStruct 映射规则中。转换结果当前符合既定业务，但状态规则的所有权不在 Service 或领域模型。 | 本轮不修改 Converter 或 Mapper 代码。纳入 V3 工程化修复：由 Service 或领域工厂显式设置初始状态和软删除标记，Converter 只负责结构化字段复制。 |

## 三、已接受的接口边界

`POST /cart/items` 与增量式 `PATCH /cart/items` 当前按“执行一次加减命令”理解，不提供请求级幂等。客户端若自动重试，同一增量可能被执行两次。之所以暂不增加 operation ID，是因为这类调用可能本来就代表两次合法操作，当前阶段只有 `POST /order` 被认定为必须完成核心幂等的高风险接口。

## 四、重新评估条件

出现以下任一情况时，再把对应事项移回开发计划：线上真实复现、客户端开始自动重试、购物车改造为总表结构、商品或商家信息开始高频并发变更、商家后台出现多端并发编辑、出现 MySQL 死锁告警、增加新的部署环境，或重复订单请求已经形成可观的数据库压力。
