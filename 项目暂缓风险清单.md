# 项目暂缓风险清单

> - 项目：Spring Boot 外卖系统 V2
> - 整理日期：2026 年 8 月 26 日
> - 范围：仅记录近期讨论中已经明确决定“暂不处理”或尚未确定最终业务语义的问题。已经完成的防超卖、商品基本状态流转、订单状态、分类约束及订单核心幂等不再列入。测试覆盖不足本身不作为本清单的风险项。

## 一、风险分类与分级口径

风险等级仅按问题一旦发生后的业务影响评定，不因发生概率低而下调；概率、现有控制措施和暂缓原因在后文单独说明。

| 风险类型 | 适用范围 |
| --- | --- |
| 业务正确性 | 订单、价格、库存、商品状态或购物车结果不符合既定业务规则。 |
| 安全 | 身份认证、授权、敏感数据、注入与越权等风险；当前没有独立归入此类的暂缓项。 |
| 一致性与并发 | 多请求、多步骤或多数据源下，数据快照、原子性、幂等性和最终状态不一致。 |
| 可用性与韧性 | 请求失败、死锁、重试、超时和故障恢复能力不足。 |
| 工程架构 | 分层边界、数据库变更管理、组件职责与实现方式带来的长期风险。 |

| 风险等级 | 影响力判定 |
| --- | --- |
| 高 | 可能造成重复订单、错单、价格或履约错误、核心数据完整性受损，或需要人工介入恢复。 |
| 中 | 可能造成单次请求失败、库存或状态边界异常，但现有事务、条件更新或最终校验能够限制影响。 |
| 低 | 不直接破坏当前核心交易正确性，主要影响实验功能、后续扩展或维护成本。 |

### 风险索引

| 编号 | 风险项 | 主类型 | 发生后的主要影响 | 等级 |
| --- | --- | --- | --- | --- |
| R-01 | 下单期间购物车被修改 | 一致性与并发 | 可能清除用户刚新增但未计入本次订单的购物车数据。 | 高 |
| R-02 | 同一 `requestId` 携带不同参数 | 业务正确性 | 可能返回并履约旧参数对应的订单，调用方无法感知冲突。 | 高 |
| R-03 | 下单校验与商品、商家状态发生竞态 | 业务正确性 | 可能使用旧价格、旧商品信息或旧营业状态创建订单。 | 高 |
| R-04 | 生产库幂等唯一索引缺少版本化迁移 | 工程架构 | 旧部署库遗漏唯一索引时可能产生重复订单。 | 高 |
| R-06 | 空购物车并发加入不同商家商品 | 一致性与并发 | 可能形成违反单商家约束的购物车。 | 中 |
| R-08 | Redis 幂等状态不进入核心流程 | 工程架构 | 限制高并发削峰和跨节点处理中状态协同能力。 | 低 |
| R-09 | Service 层包含持久化 SQL 细节 | 工程架构 | 后续调整库存 SQL 时更容易破坏事务或数据库语义。 | 低 |
| R-10 | `ProductConverter` 隐式决定商品初始状态 | 工程架构 | 默认状态规则分散，后续修改可能造成应用与数据库默认值不一致。 | 低 |
| R-12 | 商品详情 Redis 缓存为最终一致性 | 一致性与并发 | 商家商品详情可能在 TTL 内显示旧状态、库存标记或静态字段。 | 低 |
| R-14 | 商家库存重置与订单库存增减语义未统一 | 一致性与并发 | 并发后的库存结果取决于执行顺序，可能不符合商家对“重置”的理解。 | 中 |
| R-16 | 数据库缺少商品基础完整性约束 | 工程架构 | 绕过 Service 的写入可能产生负库存、未知状态码或不符合当前生命周期的状态。 | 低 |
| R-17 | 商品状态迁移依赖人工执行 | 工程架构 | 旧数据库可能继续使用旧默认值或保留不符合新规则的历史数据。 | 中 |
| R-20 | 超时任务存在吞吐上限和头部阻塞 | 可用性与韧性 | 超时订单可能持续积压，后续订单可能长期无法被扫描和取消。 | 中 |

## 二、核心订单、购物车与商家暂缓项

| 编号 | 风险项 | 当前控制与缺口 | 暂不处理的原因 |
| --- | --- | --- | --- |
| R-01 | 下单期间购物车被修改 | `createOrder` 先读取购物车并准备订单数据，之后才在事务中按条目 ID 删除。两步之间若发生增量修改，事务可能删除包含新数量的整条记录。 | 完整修复需要逐项绑定版本和数量、按数量消费，或引入购物车总表；当前发生窗口较小。 |
| R-02 | 同一 `requestId` 携带不同参数 | 当前只用 `userId + requestId` 判断同一次创建订单请求，没有保存并比较请求参数指纹。 | 正常客户端应为新的业务意图生成新 `requestId`；增加指纹会修改表结构和幂等协议。 |
| R-03 | 下单校验与商品、商家状态发生竞态 | 商品价格、名称、上下架状态和商家营业状态在订单事务外读取。库存扣减只重新约束 `status = ON_SALE` 和 `stock >= quantity`，不会绑定预检时的商品版本、价格或商家状态。因此商家在预检后改价、修改资料或打烊时，订单仍可能使用旧快照。 | 完整修复需要把结算快照、商品和商家复检、库存扣减放入统一事务，并明确价格变化时是拒单还是接受旧价。 |
| R-04 | 生产库幂等唯一索引缺少版本化迁移 | `deploy/schema.sql` 已包含 `orders(user_id, request_id)` 唯一索引，但 `CREATE TABLE IF NOT EXISTS` 不会修复已有表，项目也没有 Flyway/Liquibase 迁移来保证旧库一定补齐。 | 当前是本地练习项目，暂时接受人工核验；增加共享开发库、预发布或生产环境前必须迁移固化。 |
| R-06 | 空购物车并发加入不同商家商品 | 两个不同商家的加购请求可能同时看到购物车为空并分别写入。列表和下单阶段能够识别并阻止继续结算，但无法阻止脏购物车形成。 | 彻底解决需要用户级锁、购物车总表或更强数据库约束，当前收益不足以覆盖改造成本。 |
| R-08 | Redis 幂等状态不进入核心流程 | Redis 实验可能遇到 `PROCESSING` 超时、进程崩溃、键过期、旧请求误删新锁，以及数据库成功后 Redis 写回失败。当前核心幂等仍由数据库唯一约束保证。 | 项目当前没有重复请求压垮数据库的压力，Redis 继续作为学习实验，不替换数据库主流程。 |
| R-20 | 超时任务存在吞吐上限和头部阻塞 | 每轮固定按 `create_time, id` 查询最老的 100 条超时订单。超过每轮处理能力时会形成积压；取消持续失败的订单会回滚为 `WAIT_PAY`，下一轮继续占据最前位置。当持续失败记录达到批次上限时，后续超时订单可能永远无法进入处理批次。 | 当前订单规模较小，暂时接受固定批次扫描。扩大流量前需要增加匹配索引、失败退避或重试次数、可推进游标/跳过机制，以及积压量和最老等待时间监控。 |

## 三、商品模块暂缓项

| 编号 | 风险项 | 当前控制与缺口 | 暂不处理的原因 |
| --- | --- | --- | --- |
| R-09 | Service 层包含持久化 SQL 细节 | `ProductService` 使用 `setSql` 表达库存扣减和状态切换，列名、SQL 算术与数据库方言进入业务层。`quantity` 已经过正整数校验，因此当前不是直接的 SQL 注入漏洞。 | 原子条件扣减已经能够防止超卖，本阶段不为分层纯度改写稳定逻辑；后续可下沉为参数化 Mapper 方法。 |
| R-10 | `ProductConverter` 隐式决定商品初始状态 | 创建 DTO 转实体时由 MapStruct 直接写入 `OFF_SALE` 和 `NOT_DELETED`。数据库默认值也已改为 `OFF_SALE`，结果一致，但业务规则同时存在于转换器和 DDL。 | 当前行为正确且风险主要是维护性；后续由 Service 或领域工厂显式设置初始状态，Converter 只复制字段。 |
| R-12 | 商品详情 Redis 缓存为最终一致性 | 缓存命中直接信任整个缓存 DTO，包括 `status`、`inStock`、价格、名称、图片和描述。删除失败仅记录日志，没有重试、消息补偿或版本键；更新事务提交前删除缓存还存在旧请求重新回填旧值的窗口。TTL 为 1 小时。该缓存目前用于商家商品详情，不是用户店铺商品列表，也不参与最终库存扣减。 | Redis 作为项目中的实验技术保留，当前明确接受 TTL 内脏数据，不为该非核心读路径引入 outbox、CDC、延迟双删等复杂机制。 |
| R-14 | 商家库存重置与订单库存增减语义未统一 | 商家修改库存是带 `@Version` 的绝对值更新；下单扣减和取消退库使用 `stock = stock ± quantity` 并递增版本。`decreaseStock` 没有 `WHERE version = expectedVersion`，它是带库存条件的原子更新，不是版本号乐观锁。它能够防止超卖，但商家重置先完成时，后续订单会得到 `reset - quantity`，后续退库会得到 `reset + quantity`；订单或退库先完成时，持旧版本的商家重置会失败。 | 当前尚未确定“商家重置值必须是最终值”还是“按数据库执行顺序接受订单增减”的业务语义。在规则明确前不增加强锁、重试或库存流水模型。 |
| R-16 | 数据库缺少商品基础完整性约束 | 当前业务语义已经明确：上架展示且库存为零使用 `SALE_OUT + stock = 0`，有库存且可购买使用 `ON_SALE + stock > 0`，主动下架使用 `OFF_SALE`。Service 维护这些流转，但数据库还没有 `stock >= 0`、状态取值范围等基础 `CHECK`。 | 当前正式写入口集中在 Service，缺少约束不会破坏正常请求；数据库约束主要防御手工 SQL、迁移错误和未来新增写入口，暂按低风险保留。 |
| R-17 | 商品状态迁移依赖人工执行 | 新建库的 `deploy/schema.sql` 已将商品默认状态设为 `OFF_SALE`，并提供 `deploy/migrations/20260820_product_status_lifecycle.sql` 修正旧库默认值和历史状态，但项目没有自动迁移框架。 | 当前部署规模小，暂时接受手工执行和核验；增加第二个长期环境前，应将该脚本纳入自动迁移。 |

## 四、已接受的设计边界

- 商品详情 Redis 缓存只提供最终一致性，不作为库存事实来源；订单扣库存仍以 MySQL 条件更新结果为准。
- R-11：`POST /cart/items` 和增量式 `PATCH /cart/items` 按“执行一次增减命令”处理，不提供请求级幂等。客户端不得自动重试这类请求；若以后启用自动重试，再引入操作 ID 或改为设置最终数量的接口。
- 购物车只做体验性预检，不保证数量与实时库存强一致；提交订单是最终校验点。
- `decreaseStock` 使用业务条件原子扣减而不是版本号乐观锁。它解决超卖，但不保证与商家绝对库存重置发生版本冲突。
- 用户店铺查询展示 `ON_SALE` 和 `SALE_OUT`，购物车及下单入口仍只接受 `ON_SALE`。
- 订单退库允许给逻辑删除商品归还库存；恢复商品统一进入 `OFF_SALE` 并递增版本，必须由商家重新上架后才对用户可见。
- 商品创建在应用转换规则和新建库 DDL 中都默认为 `OFF_SALE`；旧库必须人工执行迁移。

## 五、重新评估条件

出现以下任一情况时，应把对应事项移回开发计划：线上真实复现；客户端开始自动重试；购物车改造为缓存或库存预占；商品价格、库存或商家信息出现高频并发修改；出现 MySQL 死锁告警；Redis 脏数据影响实际运营；出现绕过 Service 的商品写入口；超时订单出现积压或持续取消失败；增加共享开发库、预发布或生产环境。
