# 数据库设计检查报告

## 1. 检查结论

本文检查的是 `MysqlApiIntegrationTest.recreateSchema()` 当前创建的九张 MySQL 表，以及九个实体、Mapper、Service 和集成测试对这些表的实际使用方式。字段核对结果是：当前 MySQL 测试 DDL 已覆盖全部实体字段；主要风险已经从“测试漏字段”转为“没有正式迁移、关系约束不足”。

近期已经完成以下重要改进：

- `orders` 增加非空 `request_id` 和唯一键 `uk_orders_user_request_id(user_id, request_id)`，为订单创建幂等提供最终约束；
- `category` 增加唯一键 `uk_merchant_category(merchant_id, category_name)`，消除同一商家并发创建同名分类的数据库竞态；
- `merchant` 增加 `version`，与 `product.version`、`cart.version` 一起使用 MyBatis-Plus 乐观锁。
- 新增 `rider` 与 `delivery_task`，骑手名称和手机号唯一，一张订单最多对应一个配送任务；
- 配送任务固化商家名称、配送奖励、收货人与商家联系方式/地址，避免履约期间基础资料变化影响配送。

当前最需要继续处理的不是再增加应用层“先查后写”，而是让测试 DDL 成为可部署、可迁移的正式结构，并补齐核心关系约束。主要剩余问题如下：

| 优先级 | 问题 | 影响 |
|---|---|---|
| P0 | 完整 DDL 只存在于集成测试，没有 Flyway/Liquibase 迁移 | 无法证明开发、测试和生产使用同一结构；新增唯一键与版本列可能未部署 |
| P0 | 多数逻辑关联没有外键，且 ID 的 signed/unsigned 不一致 | 可产生孤儿行、跨商家错绑；直接加外键会因类型不一致失败 |
| P1 | 商品逻辑删除与 `(merchant_id, product_name)` 唯一键语义冲突 | 已删除商品仍占用名称，和恢复逻辑表达的“活动商品唯一”不一致 |
| P1 | “每个商家一个默认分类”没有数据库约束 | 多个默认分类会让 `selectOne()` 失败或迁移目标不确定 |
| P1 | 单商家购物车仍靠先查后插 | 并发加入不同商家商品时，数据库允许同一用户出现多商家购物车 |
| P2 | 查询索引与实际过滤/排序条件没有完全对齐 | 数据增长后出现额外扫描、排序和重复索引维护 |

## 2. 当前已经生效的关键约束

### 2.1 唯一性

- 用户：`phone`、`username` 唯一；
- 商家：`phone`、`username` 唯一；
- 分类：同一商家的 `category_name` 唯一；
- 商品：同一商家的 `product_name` 唯一，包括逻辑删除记录；
- 购物车：同一用户对同一商品只允许一行；
- 订单：`order_no` 全局唯一；同一用户的 `request_id` 唯一；
- 骑手：`name`、`phone` 唯一；
- 配送任务：`order_id` 唯一，一张订单最多创建一条任务。

其中订单、分类和一单一配送任务的唯一键都已经成为并发正确性的最终裁决点，不能只保留 Service 中的预查询而删除数据库约束。

### 2.2 乐观锁

- `merchant.version`：保护商家资料和营业状态更新；
- `product.version`：保护商品资料与状态更新；
- `cart.version`：保护购物车数量更新，减到 0 的删除也显式匹配旧版本。

`version` 解决的是旧读覆盖新写，不替代库存扣减、订单状态转换等需要条件 SQL 的场景。

### 2.3 外键

当前唯一外键是：

```text
product.category_id -> category.id
ON DELETE RESTRICT ON UPDATE RESTRICT
```

商品创建与分类删除还会对目标分类行执行 `SELECT ... FOR UPDATE`，用于串行化“验证后创建”和“迁移商品后删除”。外键负责最终完整性兜底。

## 3. 高优先级剩余问题

### 3.1 缺少版本化数据库迁移

项目没有 `schema.sql`、Flyway 或 Liquibase。`spring.sql.init.mode=never`，完整建表语句只写在测试方法中。这意味着 Java 实体和测试 DDL 已经增加的以下结构不一定存在于实际运行库：

- `merchant.version`；
- `category.uk_merchant_category`；
- `orders.request_id`；
- `orders.uk_orders_user_request_id`；
- 完整 `rider` 表及其名称、手机号唯一键；
- 完整 `delivery_task` 表、配送快照字段和 `uk_delivery_task_order_id`。

建议把当前九张表整理为基线迁移，并让集成测试和实际环境执行同一套迁移。之后每次字段、索引或约束变化都增加独立版本，不再复制一份测试专用 DDL。

### 3.2 关联 ID 类型不统一，且核心关系缺少外键

当前 `user.id`、`cart.user_id`、`cart.product_id` 是 `bigint UNSIGNED`，而 `product.id`、`orders.user_id` 等是有符号 `bigint`。MySQL 外键两端的类型和 unsigned 属性必须一致，因此需要先统一类型。

建议统一为有符号 `bigint`，再按业务保留策略增加：

| 子表字段 | 父表字段 | 建议规则 |
|---|---|---|
| `category.merchant_id` | `merchant.id` | `RESTRICT` |
| `product.(merchant_id, category_id)` | `category.(merchant_id, id)` | `RESTRICT`，同时保证分类属于该商家 |
| `cart.user_id` | `user.id` | `CASCADE` 或按用户删除策略决定 |
| `cart.(product_id, merchant_id)` | `product.(id, merchant_id)` | `RESTRICT` |
| `orders.user_id` | `user.id` | `RESTRICT`，订单作为历史凭证 |
| `orders.merchant_id` | `merchant.id` | `RESTRICT` |
| `order_item.order_id` | `orders.id` | `CASCADE` |
| `order_item.product_id` | `product.id` | `RESTRICT` 或明确不设，保留订单快照 |
| `delivery_task.order_id` | `orders.id` | `RESTRICT`，配送记录随订单历史保留 |
| `delivery_task.rider_id` | `rider.id` | `RESTRICT`；允许待接单时为 `NULL` |

若建立商家+分类复合外键，父表需先有 `UNIQUE (merchant_id, id)`；购物车采用商品+商家复合外键时，商品表同样需要 `UNIQUE (id, merchant_id)`。

### 3.3 商品逻辑删除与唯一索引冲突

`Product.isDeleted` 使用 `@TableLogic`，但当前唯一键是：

```sql
UNIQUE KEY uk_merchant_product (merchant_id, product_name)
```

因此逻辑删除后的记录仍禁止同一商家创建同名商品。与此同时，`restoreDeletedProduct` 只检查同名未删除商品，表达的是“活动商品名称唯一”。两种规则不一致。

如果业务需要“仅未删除商品唯一”，可以使用生成列：

```sql
ALTER TABLE product
    DROP INDEX uk_merchant_product,
    ADD COLUMN active_product_name varchar(100)
        GENERATED ALWAYS AS (
            CASE WHEN is_deleted = 0 THEN product_name ELSE NULL END
        ) STORED,
    ADD UNIQUE KEY uk_product_active_name
        (merchant_id, active_product_name);
```

如果业务决定商品名称删除后永远不可复用，则应保留现有唯一键，并把恢复逻辑和文档统一为这一规则。

### 3.4 默认分类唯一性未被保证

分类删除时按 `merchant_id + is_default = 0` 使用 `selectOne()` 获取迁移目标，但数据库允许同一商家存在多个默认分类。不能直接创建 `UNIQUE (merchant_id, is_default)`，否则普通分类也只能有一个。

可使用只在默认分类上产生非空值的生成列：

```sql
ALTER TABLE category
    ADD COLUMN default_merchant_id bigint
        GENERATED ALWAYS AS (
            CASE WHEN is_default = 0 THEN merchant_id ELSE NULL END
        ) STORED,
    ADD UNIQUE KEY uk_category_one_default (default_merchant_id),
    ADD CONSTRAINT chk_category_default CHECK (is_default IN (0, 1));
```

这项约束与已存在的 `uk_merchant_category(merchant_id, category_name)` 解决不同问题，两者都应保留。

### 3.5 单商家购物车仍存在先查后插竞态

当前加入商品前执行 `user_id = ? AND merchant_id <> ?` 的冲突查询。两个不同商家的商品同时加入空购物车时，两个请求都可能先看到“无冲突”并各自插入。

现有查询和下单逻辑会识别多商家并禁止购买，因此不会直接形成跨商家订单，但数据结构并未保证业务规则。推荐拆为：

```text
shopping_cart(id, user_id UNIQUE, merchant_id, ...)
shopping_cart_item(id, cart_id, product_id, quantity, ...)
```

由每个用户唯一的购物车主表固定商家，切换商家时显式清空或更换购物车。

## 4. 索引审查

### 4.1 `user` 与 `merchant`

用户名和手机号唯一索引与注册、登录、查重完全匹配。`merchant.version` 是字段，不需要单独索引，因为更新仍通过主键定位并在行上比较版本。

商家名称搜索使用前置通配符时，普通 B-Tree 无法有效加速。数据量增长后可改用前缀搜索、MySQL FULLTEXT（中文需评估 `ngram`），或专门搜索服务。

### 4.2 `category`

`uk_merchant_category(merchant_id, category_name)` 的左前缀已经支持仅按 `merchant_id` 查询，因此现有 `idx_merchant_id(merchant_id)` 是重复索引。

如果商家分类列表数据量很大，并且始终带 `status`，可使用：

```sql
ADD KEY idx_category_merchant_status (merchant_id, status);
DROP INDEX idx_merchant_id;
```

分类名称唯一键继续保留，不能用普通查询索引替代。

### 4.3 `product`

商品列表主要使用两组条件：

```text
merchant_id + is_deleted + [status]
merchant_id + is_deleted + [category_id]
```

建议增加：

```sql
ADD KEY idx_product_merchant_deleted_status
    (merchant_id, is_deleted, status),
ADD KEY idx_product_merchant_deleted_category
    (merchant_id, is_deleted, category_id);
```

新增后可评估删除单列 `idx_merchant_id`。`idx_category_id` 应保留以支持现有外键和分类删除时的商品迁移。

### 4.4 `cart`

`uk_user_product(user_id, product_id)` 已支持按用户+商品定位、仅按用户查询，以及 `INSERT ... ON DUPLICATE KEY UPDATE`。因此 `idx_user_id(user_id)` 被唯一键左前缀覆盖，可以删除。

`idx_product_id(product_id)` 当前业务查询较少，但如果补商品外键，应保留对应索引；若改为复合外键，则替换为 `(product_id, merchant_id)`。

### 4.5 `orders`

`uk_orders_user_request_id(user_id, request_id)` 支持幂等查询，但订单列表实际执行：

```text
WHERE user_id = ? ORDER BY create_time DESC
```

建议将单列 `idx_order_user` 替换为：

```sql
ADD KEY idx_order_user_created (user_id, create_time DESC, id DESC);
```

同时代码查询应以 `id DESC` 作为相同创建时间下的稳定次序。幂等唯一键必须继续保留；它和列表排序索引用途不同。

`idx_order_merchant` 是后续商家订单列表的预留索引；如果该列表也按时间倒序，应升级为 `(merchant_id, create_time DESC, id DESC)`。

### 4.6 `order_item`

`idx_order_item_order(order_id)` 与订单详情和批量订单明细查询匹配。如果业务保证同一订单中同一商品只出现一次，可升级为 `UNIQUE (order_id, product_id)`，其左前缀仍支持按订单查询。

未来若引入 SKU、规格或加料，同一商品可能合法出现多行，此时应在更细的业务维度建唯一键。

### 4.7 `rider` 与 `delivery_task`

`uk_rider_name`、`uk_rider_phone` 与注册、登录查询匹配。骑手任务查询目前主要使用：

```text
可接任务：status + rider_id IS NULL
当前任务：rider_id + status
任务详情：id + rider_id
```

建议增加 `delivery_task(status, rider_id, id)` 和 `delivery_task(rider_id, status, id)`，并为列表查询增加稳定的 `id` 或 `create_time` 排序。`uk_delivery_task_order_id` 继续用于一单一任务，不能由普通查询索引替代。

## 5. 其他完整性问题

### 5.1 建议增加 CHECK 约束

DTO 校验无法保护手工 SQL、测试脚本或后台任务。建议至少验证：

```text
product:    price > 0, stock >= 0, is_deleted IN (0,1), status IN (0,1,2)
cart:       quantity > 0, price >= 0
category:   status IN (0,1), is_default IN (0,1)
merchant:   status IN (0,1), version >= 0, opening_time <> closing_time
orders:     status IN (0,1,2,3,4,5,6,7,8), request_id <> '', 各金额 >= 0
order_item: product_price >= 0, quantity > 0, subtotal >= 0
rider:      status IN (0,1), is_delete IN (0,1)
delivery_task: status IN (0,1,2), delivery_reward >= 0
```

订单当前九态为：`0 WAIT_PAY`、`1 PAYING`、`2 PAID`、`3 FINISHED`、`4 CANCELLED`、`5 PREPARING`、`6 READY`、`7 DELIVERING`、`8 DELIVERED`。旧的 `0..4` 约束或文档已经不适用。

金额等式是否直接放入 CHECK，需要结合未来优惠分摊与舍入规则决定。

### 5.2 金额精度

商品单价、订单金额和明细小计都使用 `decimal(10,2)`。订单总额是多行小计之和，理论上需要比单价更多整数位。可保留单价 `decimal(10,2)`，将订单金额和明细小计提升为 `decimal(12,2)`，或按业务上限确定。

### 5.3 商家审计时间

`merchant` 已有 `version`，但仍没有 `update_time`。商家名称、地址、电话、描述、图片和营业时间都可修改，建议增加自动更新时间以支持审计和问题排查。

### 5.4 购物车价格语义

购物车保存商品名称、图片和加入时价格；展示时使用购物车快照，下单时重新读取 `product.price`。商品改价后可能出现购物车展示价与最终下单价不同。

需要明确选择：

- 展示实时价：查询购物车时读取商品当前价格；
- 锁定加入价：下单也使用购物车价格，并增加价格有效期；
- 常见折中：购物车展示实时价，订单明细在下单时固化最终成交快照。

### 5.5 API 长度与表字段

`requestId` 已与数据库统一为最长 64。当前仍有多组 API 与 DDL 上限不同：商家用户名 `50/100`、分类名称 `15/255`、商品名称 `15/100`、商品描述 `100/500`、骑手名称 `50/255`（前者为 API，后者为 DDL）；`orders.receiver_name` 是 20、`receiver_address` 是 255，而 DTO 没有声明相同上限。较宽的数据库字段不会让正常 API 写入失败，但手工 SQL 和其他入口可以写入 API 无法再次提交的数据。应确定最终业务上限，并同时固化到 DTO 校验和数据库列/CHECK 中。

## 6. V3 工程化建设（推荐实施顺序）

当前项目仅作为本地练习项目运行，V2 明确接受手工维护数据库、在测试代码中内嵌 DDL，以及部分测试依赖既有表结构；该接受不适用于 CI、共享开发库、预发布或生产环境。出现任一非本地环境时，应先完成本节前四项，不能继续依赖人工核对。

1. 选择 Flyway 或 Liquibase，把 `MysqlApiIntegrationTest.recreateSchema()` 中的九表 DDL 提取为版本化基线；不要再维护第二份 `schema.sql`。
2. 让应用、MySQL 集成测试和 Redis profile 下访问数据库的测试复用同一套迁移，消除测试顺序和本机残留 schema 依赖；测试数据继续由 fixture 准备，不新增通用 `data.sql`。
3. 增加自动 schema 漂移校验，至少核验表、列、类型、非空、默认值、唯一键、外键和版本列；在 CI 中从空库执行迁移后再运行测试。
4. 在迁移脚本中确认 `merchant.version`、分类唯一键、订单幂等字段/唯一键、骑手表和配送任务快照字段已经部署。
5. 统一主键生成策略：九表 DDL 均为 `AUTO_INCREMENT`，但部分实体没有显式使用 `IdType.AUTO`，当前会依赖 MyBatis-Plus 全局默认策略。
6. 清理孤儿数据和重复数据，统一关联 ID 的 signed/unsigned。
7. 增加默认分类唯一约束和核心外键，优先保证商家、分类、商品、订单、骑手与配送任务关系。
8. 明确商品逻辑删除后的名称复用规则，再调整商品唯一键。
9. 决定是否重构为购物车主表，以数据库方式保证单商家购物车。
10. 按实际查询增加联合索引并删除被左前缀覆盖的重复索引。
11. 增加基础 CHECK、`merchant.update_time`，并统一 DTO 长度。
12. 明确购物车价格规则，并使用接近生产规模的数据执行 `EXPLAIN ANALYZE`。

增加外键、唯一键或 CHECK 前必须先检查历史数据；否则迁移会因孤儿行、重复行或非法值失败。
