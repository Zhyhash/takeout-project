# 数据库设计检查报告

## 1. 检查结论

当前数据库结构可以支撑现有功能和小数据量运行，主键、账号唯一性、订单号唯一性、
购物车幂等写入、商品到分类的外键都已经具备。

但若按生产数据库标准评估，结构仍不完整，建议优先处理以下问题：

1. 统一所有关联 ID 的有符号/无符号类型，并补齐核心外键。
2. 修正商品逻辑删除与商品名称唯一索引之间的语义冲突。
3. 用数据库约束保证“同一商家分类名唯一”和“每个商家只有一个默认分类”。
4. 将用户订单索引调整为匹配 `WHERE user_id = ? ORDER BY create_time DESC`。
5. 为商品列表的实际组合条件增加联合索引。
6. 将测试类中的 DDL 迁移到版本化数据库迁移脚本。

总体判断：**概念模型基本合理，但完整性约束和查询型联合索引不足；适合开发/测试，
不建议不经调整直接作为生产结构。**

## 2. 实际调用检查范围

本报告不仅检查建表 SQL，还检查了项目中的 MyBatis-Plus Wrapper、Mapper 注解 SQL、
XML SQL 和直接使用 `JdbcTemplate` 的校验逻辑。

主要调用依据如下：

| 业务 | 实际查询/写入条件 | 代码位置 |
|---|---|---|
| 用户注册、登录 | `user.username = ?` | `UserService.java:34-36, 55-56` |
| 用户手机号查重 | `user.phone = ?` | `UniquePhoneValidator.java:30-32` |
| 商家注册、登录 | `merchant.username = ?` | `MerchantService.java:48-49, 71-72` |
| 商家手机号查重 | `merchant.phone = ?` | `UniquePhoneValidator.java:30-32` |
| 商家搜索 | `merchant_name LIKE '%关键字%' AND status = ?` | `MerchantQueryService.java:56-58` |
| 商家分类列表 | `merchant_id = ? AND status = ?` | `CategoryService.java:43-45` |
| 分类防重 | `merchant_id = ? AND category_name = ? AND status = ?` | `CategoryService.java:98-101` |
| 默认分类 | `merchant_id = ? AND is_default = 0` | `CategoryService.java:78-82` |
| 删除分类前迁移商品 | `category_id = ? AND merchant_id = ?` | `CategoryService.java:86-92` |
| 商家商品列表 | `merchant_id = ? AND is_deleted = 0`，可选 `status`、`category_id` | `ProductMapper.xml` |
| 用户端店铺商品 | `merchant_id = ? AND status = 0`，逻辑删除条件由 MyBatis-Plus 注入 | `MerchantQueryService.java:140-142` |
| 购物车查询/清空 | `user_id = ?` | `CartService.java:199-200, 293-294` |
| 购物车幂等新增 | 唯一键冲突后 `quantity = quantity + 1` | `CartMapper.java:13-32` |
| 同商品购物车条目 | `user_id = ? AND product_id = ?` | `CartService.java:82-84` |
| 用户订单列表 | `user_id = ? ORDER BY create_time DESC` | `OrderService.java:148-149` |
| 订单详情/状态流转 | `id = ? AND user_id = ? [AND status = ?]` | `OrderDomainService.java:30-33` |
| 订单明细 | `order_id = ?` 或 `order_id IN (...)` | `OrderService.java:136-137, 156-159` |

## 3. 高优先级问题

### 3.1 关联 ID 类型不统一，且核心关系缺少外键

当前 ID 类型同时存在 `bigint` 和 `bigint UNSIGNED`：

- `user.id` 是 `bigint UNSIGNED`，`orders.user_id` 却是 `bigint`。
- `product.id` 是 `bigint`，`cart.product_id` 却是 `bigint UNSIGNED`。
- `cart.id` 是 `bigint UNSIGNED`，其余多数主键是 `bigint`。

MySQL 外键两侧的整数类型及 signed/unsigned 属性必须兼容。因此，
`cart.product_id -> product.id` 当前不能直接创建外键。

项目是单体应用、同一 MySQL 库、事务内写入，外键能明显降低孤儿数据风险，适合使用。
建议统一为 **`bigint`（有符号）**，与 Java `Long` 的可表示范围一致。

建议的关系与删除策略：

| 子表字段 | 父表字段 | 建议删除策略 | 原因 |
|---|---|---|---|
| `category.merchant_id` | `merchant.id` | `RESTRICT` | 商家仍有分类时不应物理删除 |
| `product.(merchant_id, category_id)` | `category.(merchant_id, id)` | `RESTRICT` | 同时保证分类存在且确属该商家 |
| `cart.user_id` | `user.id` | `CASCADE` | 物理删除用户时购物车无保留价值 |
| `cart.(product_id, merchant_id)` | `product.(id, merchant_id)` | `RESTRICT` 或清理后删除 | 保证购物车商品和商家匹配 |
| `orders.user_id` | `user.id` | `RESTRICT` | 订单是历史凭证，不应级联删除 |
| `orders.merchant_id` | `merchant.id` | `RESTRICT` | 订单是历史凭证，不应级联删除 |
| `order_item.order_id` | `orders.id` | `CASCADE` | 订单被明确物理删除时明细应一起删除 |
| `order_item.product_id` | `product.id` | `RESTRICT` 或明确不设 | 当前取消订单需要商品存在以归还库存 |

其中，商品和分类只建立 `product.category_id -> category.id` 仍不足以阻止
“A 商家的商品挂到 B 商家的分类”。推荐增加：

```sql
ALTER TABLE category
    ADD UNIQUE KEY uk_category_merchant_id (merchant_id, id);

ALTER TABLE product
    ADD CONSTRAINT fk_product_category_merchant
    FOREIGN KEY (merchant_id, category_id)
    REFERENCES category (merchant_id, id)
    ON DELETE RESTRICT ON UPDATE RESTRICT;
```

购物车保留冗余的 `merchant_id` 时，也应使用相同思路保证商品与商家一致；
否则可删除 `cart.merchant_id`，在需要时从 `product` 获取。若采用
`cart.(product_id, merchant_id) -> product.(id, merchant_id)`，父表还需增加
`UNIQUE (id, merchant_id)`，子表需增加同顺序的 `(product_id, merchant_id)` 索引。

### 3.2 商品逻辑删除与唯一索引语义冲突

`product` 使用 `@TableLogic` 和 `is_deleted`，说明商品采用逻辑删除；但当前唯一索引是：

```sql
UNIQUE KEY uk_merchant_product (merchant_id, product_name)
```

这会导致商品逻辑删除后，同一商家仍然不能创建同名商品。与此同时，
`restoreDeletedProduct` 又检查“是否存在同名且未删除商品”，表明代码实际想表达的是：

> 同一商家只能有一个“未删除”的同名商品，已删除记录不参与唯一性。

两者语义不一致。MySQL 没有通用的 `WHERE is_deleted = 0` 部分唯一索引，
可使用生成列解决：

```sql
ALTER TABLE product
    DROP INDEX uk_merchant_product,
    ADD COLUMN active_product_name varchar(100)
        GENERATED ALWAYS AS (
            CASE WHEN is_deleted = 0 THEN product_name ELSE NULL END
        ) STORED,
    ADD UNIQUE KEY uk_product_active_name
        (merchant_id, active_product_name);
```

MySQL 唯一索引允许多行 `NULL`，因此多个已删除的同名历史记录可以共存，
但未删除记录仍然只能有一条。

如果业务决定“商品名称删除后永远不可复用”，则现有唯一索引可以保留，
但应删除恢复 SQL 中多余的活动商品重名判断，并把该规则写入业务文档。

### 3.3 分类唯一性只由“先查后插”保证，存在并发竞态

创建分类先查询：

```text
merchant_id = ? AND category_name = ? AND status = 0
```

然后再插入。两个并发请求都可能在查询阶段得到“不存在”，最终插入重复分类。
应增加数据库唯一约束：

```sql
ALTER TABLE category
    ADD UNIQUE KEY uk_category_merchant_name (merchant_id, category_name);
```

当前分类是物理删除，因此唯一键不需要像商品一样处理逻辑删除。如果将来分类也改成
逻辑删除，应采用与商品相同的生成列方案。

### 3.4 “每个商家只有一个默认分类”未被数据库保证

商家注册会创建默认分类，删除分类时通过：

```text
merchant_id = ? AND is_default = 0
```

使用 `selectOne()` 查询默认分类。但数据库允许同一商家插入多个 `is_default = 0`
的分类，届时 `selectOne()` 会因返回多行而失败。

不能直接建立 `UNIQUE (merchant_id, is_default)`，因为这会错误地限制每个商家只能有
一个普通分类。推荐生成列：

```sql
ALTER TABLE category
    ADD COLUMN default_merchant_id bigint
        GENERATED ALWAYS AS (
            CASE WHEN is_default = 0 THEN merchant_id ELSE NULL END
        ) STORED,
    ADD UNIQUE KEY uk_category_one_default (default_merchant_id);
```

同时建议增加：

```sql
CHECK (is_default IN (0, 1))
```

## 4. 索引审查

### 4.1 `user`

现有 `uk_user_username` 和 `uk_user_phone` 与注册、登录、手机号查重完全匹配。
无需新增索引。

### 4.2 `merchant`

现有用户名、手机号唯一索引与调用匹配。

商家搜索使用 `LIKE '%关键字%'`，普通 B-Tree 索引无法有效加速前置通配符查询。
数据量较小时可以接受；数据量增大后可选择：

- 改成前缀搜索 `LIKE '关键字%'` 并建立 `(status, merchant_name)`；
- 或使用 MySQL `FULLTEXT`（中文需评估 `ngram` 分词）；
- 或接入专门的搜索服务。

仅给 `status` 建索引通常收益有限，因为该字段只有两个低区分度值。

### 4.3 `category`

现有 `idx_merchant_id(merchant_id)` 能找到商家的分类，但不能完整覆盖真实组合条件。
建议：

```sql
ALTER TABLE category
    ADD UNIQUE KEY uk_category_merchant_name (merchant_id, category_name),
    ADD KEY idx_category_merchant_status (merchant_id, status);
```

增加联合索引后，原 `idx_merchant_id` 被左前缀覆盖，可以删除，避免重复维护。
默认分类查询由 `uk_category_one_default` 直接支持。

### 4.4 `product`

商品列表的实际条件有两组：

```text
merchant_id + is_deleted + [status]
merchant_id + is_deleted + [category_id]
```

由于 `status` 和 `category_id` 都是可选参数，只建一个四列索引会出现中间列缺失，
无法同时良好支持两种查询。建议：

```sql
ALTER TABLE product
    ADD KEY idx_product_merchant_deleted_status
        (merchant_id, is_deleted, status),
    ADD KEY idx_product_merchant_deleted_category
        (merchant_id, is_deleted, category_id);
```

`idx_category_id(category_id)` 应保留，用于现有分类外键以及删除分类前批量迁移商品。
新增上述联合索引后，单列 `idx_merchant_id` 可以删除。

不建议给 `status`、`is_deleted` 单独建索引，这两个字段区分度低。

### 4.5 `cart`

`uk_user_product(user_id, product_id)` 同时支持：

- `user_id = ? AND product_id = ?`；
- 仅按 `user_id = ?` 查询或清空；
- `INSERT ... ON DUPLICATE KEY UPDATE`。

因此 `idx_user_id(user_id)` 是重复索引，可以删除。

`idx_product_id(product_id)` 当前没有直接业务查询，但若补
`cart.product_id -> product.id` 外键，应保留对应索引；采用
`(product_id, merchant_id)` 复合外键时，应将它替换为同顺序的复合索引。

另一个未由数据库保证的规则是“一个用户的购物车只能属于一个商家”。
当前代码通过 `user_id = ? AND merchant_id <> ?` 先查后插，并发下仍可能插入两个商家。
如果该规则必须绝对可靠，推荐拆成：

```text
shopping_cart(id, user_id UNIQUE, merchant_id, ...)
shopping_cart_item(id, cart_id, product_id, quantity, ...)
```

由购物车主表固定商家，比在每条明细上重复 `merchant_id` 更容易保证一致性。

### 4.6 `orders`

订单列表实际执行：

```sql
WHERE user_id = ?
ORDER BY create_time DESC
```

现有 `idx_order_user(user_id)` 只能过滤用户，之后仍需排序。建议替换为：

```sql
ALTER TABLE orders
    DROP INDEX idx_order_user,
    ADD KEY idx_order_user_created
        (user_id, create_time DESC, id DESC);
```

加入 `id DESC` 可让相同创建时间的分页顺序稳定。当前代码也应显式增加
`id DESC` 作为第二排序字段。

项目目前没有按商家查询订单的调用，`idx_order_merchant` 是面向后续商家订单列表的
预留索引。若后续查询同样按创建时间倒序，可调整成：

```sql
(merchant_id, create_time DESC, id DESC)
```

订单状态流转使用主键 `id` 定位，再校验 `user_id/status/version`；主键已将候选行缩小
到一条，不需要额外建立 `(id, user_id, status)` 索引。

### 4.7 `order_item`

`idx_order_item_order(order_id)` 与订单详情、批量加载订单明细完全匹配。

当前数据模型中，一个订单不会出现两个相同 `product_id` 的明细，可考虑加强为：

```sql
ALTER TABLE order_item
    DROP INDEX idx_order_item_order,
    ADD UNIQUE KEY uk_order_item_product (order_id, product_id);
```

该唯一索引的左前缀仍可支持所有按 `order_id` 的查询。如果未来允许同一商品按不同
规格、加料或套餐拆成多行，应改为对 SKU/规格维度建立唯一约束，而不是直接使用上述键。

`idx_order_item_product` 当前没有直接查询，但如果添加商品外键或需要按商品追溯订单，
应保留。

## 5. 其他结构与数据完整性问题

### 5.1 建议增加 CHECK 约束

DTO 校验只能保护正常 API 请求，测试脚本、后台任务和手工 SQL 仍可写入非法数据。
MySQL 8 已实际执行 `CHECK`，建议至少增加：

```sql
product:
    CHECK (price > 0)
    CHECK (stock >= 0)
    CHECK (is_deleted IN (0, 1))
    CHECK (status IN (0, 1, 2))

cart:
    CHECK (quantity > 0)
    CHECK (price >= 0)

category:
    CHECK (status IN (0, 1))
    CHECK (is_default IN (0, 1))

merchant:
    CHECK (status IN (0, 1))
    CHECK (opening_time <> closing_time)

orders:
    CHECK (status IN (0, 1, 2, 3))
    CHECK (total_amount >= 0)
    CHECK (original_amount >= 0)
    CHECK (discount_amount >= 0)
    CHECK (total_amount = original_amount - discount_amount)

order_item:
    CHECK (product_price >= 0)
    CHECK (quantity > 0)
    CHECK (subtotal = product_price * quantity)
```

最后两条金额等式是否直接放在数据库中，应结合未来优惠分摊、舍入规则再决定。

### 5.2 金额精度上限偏紧

商品和订单金额都使用 `decimal(10,2)`，最大值为 `99,999,999.99`。
订单总额是多条商品小计之和，理论上比单价需要更大的整数位。

建议商品单价可保留 `decimal(10,2)`，订单金额和明细小计改为
`decimal(12,2)` 或按业务规模选定更大精度。

### 5.3 商家缺少 `update_time`

`merchant` 只有 `create_time`，但支持修改名称、地址、电话、描述、图片和营业时间。
建议增加：

```sql
update_time datetime NOT NULL
    DEFAULT CURRENT_TIMESTAMP
    ON UPDATE CURRENT_TIMESTAMP
```

便于审计和排查资料变更。

### 5.4 购物车快照语义不一致

`cart` 保存了商品名称、图片和加入时价格；展示购物车时使用这些快照，
创建订单时却重新读取 `product.price` 计算订单金额。因此商品改价后可能出现：

```text
购物车展示金额 != 最终下单金额
```

这不是单纯的索引问题，需要明确业务规则：

- 如果购物车显示实时价，查询时应使用商品表当前价格，快照字段可以删除或刷新；
- 如果购物车锁定加入时价格，创建订单也应使用购物车价格，并设计价格有效期；
- 常见做法是购物车显示实时价，订单明细在下单时保存最终成交快照。

`orders.merchant_name` 和 `order_item` 中的商品名称、价格、图片属于历史订单快照，
保留这些冗余是合理的。

### 5.5 API 长度校验与字段长度没有完全对齐

例如：

- `orders.receiver_name` 只有 20 字符，但 `CreateOrderDTO.receiverName` 没有 `@Size(max=20)`；
- `receiver_address` 为 255 字符，但 DTO 没有对应上限；
- 商家名称、地址、图片 URL 等字段也缺少与数据库一致的长度校验。

超长输入会在数据库层以截断或异常形式失败，错误信息不够业务化。
建议在 DTO 中同步字段上限，数据库长度仍作为最后防线。

### 5.6 表结构只存在于集成测试，缺少版本化迁移

项目中没有 `schema.sql`、数据库迁移目录，也没有 Flyway/Liquibase 依赖或配置。
完整 DDL 只写在 `MysqlApiIntegrationTest.recreateSchema()` 中，并且测试配置显式使用
`spring.sql.init.mode=never`。

这会带来以下风险：

- 测试库、开发库和生产库无法确认是否使用同一版本的结构；
- 修改 Java 实体或测试 DDL 时，没有可审计、可回滚的数据库迁移记录；
- 新环境无法可靠地从零初始化；
- 索引、外键或字段变更容易只在某个环境手工执行。

建议引入 Flyway 或 Liquibase，将当前基线结构整理为首个版本，再将本报告确认后的
约束和索引调整按独立版本迁移。集成测试应执行同一套迁移，而不是维护第二份 DDL。

## 6. 推荐实施顺序

1. 清理现有孤儿数据和重复数据。
2. 统一所有关联 ID 为 `bigint`，解决 signed/unsigned 不一致。
3. 增加分类名称唯一、唯一默认分类以及商品活动名称唯一约束。
4. 增加外键；先加父表所需唯一索引，再加子表外键。
5. 按实际调用增加商品、订单联合索引，并删除被左前缀覆盖的重复索引。
6. 增加基础 `CHECK` 约束和 `merchant.update_time`。
7. 明确购物车价格语义，再决定是否保留购物车商品快照字段。
8. 将最终结构写入 Flyway/Liquibase 基线和增量迁移，并让集成测试复用迁移。
9. 使用 `EXPLAIN ANALYZE` 和接近生产规模的数据验证索引选择。

外键和唯一约束上线前必须先执行数据检查；否则迁移会因历史孤儿行或重复行失败。

## 7. 建议保留、调整与新增汇总

| 表 | 保留 | 调整/删除 | 新增 |
|---|---|---|---|
| `user` | 用户名、手机号唯一索引 | `id` 统一为 `bigint` | 被引用外键 |
| `merchant` | 用户名、手机号唯一索引 | 无 | `update_time`、被引用外键 |
| `category` | 主键 | 删除重复的单列商家索引（新增联合索引后） | 商家外键、商家+名称唯一、商家+状态索引、唯一默认分类 |
| `product` | 分类方向索引 | 修正活动商品唯一语义，删除单列商家索引 | 两组商家可见商品联合索引、商家+分类复合外键 |
| `cart` | 用户+商品唯一索引 | 删除重复的 `idx_user_id`，统一 ID 类型 | 用户外键、商品+商家复合约束 |
| `orders` | 订单号唯一索引 | 用户单列索引升级为用户+创建时间 | 用户/商家外键 |
| `order_item` | 商品方向索引 | 订单普通索引可升级为订单+商品唯一索引 | 订单外键，按策略决定商品外键 |
