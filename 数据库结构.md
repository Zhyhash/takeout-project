# 数据库结构

## 1. 文档依据

本文档按当前九个实体类与 `src/test/java/org/example/takeout/integration/MysqlApiIntegrationTest.java`
中 `recreateSchema()` 的实际建表语句交叉核对后整理。字段集合以实体为准，类型、默认值、索引和约束以 MySQL 测试 DDL 为准；改进建议另见《数据库设计检查报告》。

- 数据库：MySQL 8（建表语句使用 `utf8mb4_0900_ai_ci`）
- 存储引擎：InnoDB
- 字符集：`utf8mb4`
- 排序规则：`utf8mb4_0900_ai_ci`
- 表数量：9

本次核对确认 `user`、`merchant`、`category`、`product`、`cart`、`orders`、`order_item`、`rider`、`delivery_task` 的 DDL 字段均覆盖对应实体字段。项目仍没有 Flyway/Liquibase，因此这里记录的是当前代码所要求的结构，不代表结构已自动部署到实际运行库。

## 2. 表关系概览

当前只有一条真正由数据库强制执行的外键：

```text
category 1 ─── N product
             product.category_id -> category.id
```

代码中还存在下列逻辑关系，但当前建表语句没有定义外键：

```text
merchant 1 ─── N category
merchant 1 ─── N product
user     1 ─── N cart
product  1 ─── N cart
merchant 1 ─── N cart
user     1 ─── N orders
merchant 1 ─── N orders
orders   1 ─── N order_item
product  1 ─── N order_item
rider    1 ─── N delivery_task
orders   1 ─── 0..1 delivery_task
```

`delivery_task.order_id` 有唯一索引，测试结构因此保证一张订单最多对应一条配送任务；`rider_id` 和 `order_id` 目前都没有外键。

## 3. `user` 用户表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint UNSIGNED` | 否 | `AUTO_INCREMENT` | 用户主键 |
| `phone` | `varchar(20)` | 否 | 无 | 手机号 |
| `password` | `varchar(255)` | 否 | 无 | 密码哈希 |
| `nickname` | `varchar(50)` | 否 | `''` | 用户昵称 |
| `email` | `varchar(100)` | 是 | `NULL` | 邮箱 |
| `status` | `tinyint` | 否 | `1` | `1` 正常、`0` 禁用、`2` 未激活、`-1` 已注销 |
| `create_time` | `datetime` | 否 | `CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `datetime` | 否 | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `username` | `varchar(50)` | 否 | 无 | 登录用户名 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_user_phone` | 唯一索引 | `phone` | 保证手机号唯一、支持手机号查重 |
| `uk_user_username` | 唯一索引 | `username` | 保证用户名唯一、支持注册和登录查询 |

### 外键

无。

## 4. `merchant` 商家表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 商家主键 |
| `username` | `varchar(100)` | 否 | 无 | 商家登录用户名；注册/登录 API 最长 50 个字符 |
| `phone` | `varchar(20)` | 否 | 无 | 商家手机号 |
| `password` | `varchar(100)` | 否 | 无 | 密码哈希 |
| `address` | `varchar(255)` | 否 | `''` | 商家地址 |
| `status` | `tinyint` | 否 | `0` | `0` 营业、`1` 打烊 |
| `create_time` | `datetime` | 否 | `CURRENT_TIMESTAMP` | 创建时间 |
| `merchant_name` | `varchar(255)` | 否 | 无 | 店铺名称 |
| `picture` | `varchar(255)` | 是 | `NULL` | 店铺图片 URL |
| `merchant_description` | `varchar(255)` | 是 | `NULL` | 店铺描述 |
| `opening_time` | `time` | 否 | `'08:00:00'` | 开始营业时间 |
| `closing_time` | `time` | 否 | `'22:00:00'` | 结束营业时间 |
| `version` | `int` | 否 | `0` | MyBatis-Plus 乐观锁版本号 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_merchant_phone` | 唯一索引 | `phone` | 保证手机号唯一、支持手机号查重 |
| `uk_merchant_username` | 唯一索引 | `username` | 保证用户名唯一、支持注册和登录查询 |

### 外键

无。

## 5. `category` 商品分类表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 分类主键 |
| `merchant_id` | `bigint` | 否 | 无 | 所属商家 ID |
| `category_name` | `varchar(255)` | 否 | 无 | 分类名称；创建 API 最长 15 个字符 |
| `status` | `tinyint` | 否 | `0` | `0` 正常使用、`1` 非法/停用分类 |
| `is_default` | `tinyint` | 否 | `0` | `0` 默认分类、`1` 商家自建分类 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_merchant_category` | 唯一索引 | `merchant_id, category_name` | 保证同一商家的分类名称唯一，并发创建时由数据库兜底 |
| `idx_merchant_id` | 普通索引 | `merchant_id` | 按商家查询分类 |

### 外键

无。`merchant_id` 在代码中指向 `merchant.id`，但数据库未强制约束。

## 6. `product` 商品表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 商品主键 |
| `category_id` | `bigint` | 否 | 无 | 所属分类 ID |
| `product_name` | `varchar(100)` | 否 | 无 | 商品名称；创建 API 最长 15 个字符 |
| `image_url` | `varchar(255)` | 是 | `NULL` | 商品图片 URL |
| `price` | `decimal(10,2)` | 否 | 无 | 商品价格 |
| `stock` | `int` | 否 | 无 | 库存数量 |
| `merchant_id` | `bigint` | 否 | 无 | 所属商家 ID |
| `is_deleted` | `tinyint` | 否 | `0` | 逻辑删除标记：`0` 未删除、`1` 已删除 |
| `status` | `tinyint` | 否 | `0` | `0` 在售、`1` 下架、`2` 售罄 |
| `description` | `varchar(500)` | 是 | `NULL` | 商品描述；创建 API 最长 100 个字符 |
| `create_time` | `datetime` | 否 | `CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `datetime` | 否 | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `version` | `int` | 否 | `0` | MyBatis-Plus 乐观锁版本号 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `idx_category_id` | 普通索引 | `category_id` | 按分类查询/更新商品，并支持分类外键 |
| `idx_merchant_id` | 普通索引 | `merchant_id` | 按商家查询商品 |
| `uk_merchant_product` | 唯一索引 | `merchant_id, product_name` | 同一商家下商品名称全局唯一 |

### 外键

| 外键名 | 本表字段 | 引用字段 | 删除规则 | 更新规则 |
|---|---|---|---|---|
| `fk_product_category` | `category_id` | `category.id` | `RESTRICT` | `RESTRICT` |

## 7. `cart` 购物车表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint UNSIGNED` | 否 | `AUTO_INCREMENT` | 购物车条目主键 |
| `user_id` | `bigint UNSIGNED` | 否 | 无 | 用户 ID |
| `product_id` | `bigint UNSIGNED` | 否 | 无 | 商品 ID |
| `product_name` | `varchar(255)` | 否 | 无 | 加入购物车时保存的商品名称 |
| `quantity` | `int` | 否 | `1` | 商品数量 |
| `price` | `decimal(10,2)` | 否 | 无 | 加入购物车时保存的商品价格 |
| `create_time` | `datetime` | 否 | `CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `datetime` | 否 | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `merchant_id` | `bigint` | 否 | 无 | 商品所属商家 ID |
| `product_image` | `varchar(255)` | 否 | 无 | 加入购物车时保存的商品图片 |
| `version` | `int` | 否 | `0` | MyBatis-Plus 乐观锁版本号 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_user_product` | 唯一索引 | `user_id, product_id` | 一个用户对同一商品只能有一条购物车记录；支持 `ON DUPLICATE KEY UPDATE` |
| `idx_user_id` | 普通索引 | `user_id` | 按用户查询/清空购物车 |
| `idx_product_id` | 普通索引 | `product_id` | 按商品定位购物车条目 |

### 外键

无。`user_id`、`product_id`、`merchant_id` 都只是代码层逻辑引用。

## 8. `orders` 订单主表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 订单主键 |
| `order_no` | `varchar(32)` | 否 | 无 | 对外订单号 |
| `user_id` | `bigint` | 否 | 无 | 下单用户 ID |
| `request_id` | `varchar(64)` | 否 | 无 | 客户端一次下单意图的标识，在用户范围内用于幂等 |
| `merchant_id` | `bigint` | 否 | 无 | 商家 ID |
| `merchant_name` | `varchar(255)` | 否 | 无 | 下单时的商家名称快照 |
| `total_amount` | `decimal(10,2)` | 否 | 无 | 实付/订单总金额 |
| `status` | `tinyint` | 否 | `0` | `0` 待支付、`1` 支付中、`2` 已支付、`3` 已完成、`4` 已取消、`5` 制作中、`6` 待骑手、`7` 配送中、`8` 已送达 |
| `receiver_name` | `varchar(20)` | 否 | 无 | 收货人姓名 |
| `receiver_phone` | `varchar(20)` | 否 | 无 | 收货人手机号 |
| `receiver_address` | `varchar(255)` | 否 | 无 | 收货地址 |
| `remark` | `varchar(255)` | 是 | `NULL` | 订单备注 |
| `create_time` | `datetime` | 否 | `CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `datetime` | 否 | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `finish_time` | `datetime` | 是 | `NULL` | 完成时间 |
| `original_amount` | `decimal(10,2)` | 否 | `0.00` | 原始金额 |
| `discount_amount` | `decimal(10,2)` | 否 | `0.00` | 优惠金额 |
| `pay_time` | `datetime` | 是 | `NULL` | 支付时间 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_order_no` | 唯一索引 | `order_no` | 保证业务订单号唯一 |
| `uk_orders_user_request_id` | 唯一索引 | `user_id, request_id` | 保证同一用户的同一次下单意图最多形成一张订单 |
| `idx_order_user` | 普通索引 | `user_id` | 按用户查询订单 |
| `idx_order_merchant` | 普通索引 | `merchant_id` | 按商家查询订单 |

### 外键

无。`user_id` 和 `merchant_id` 都只是代码层逻辑引用。

## 9. `order_item` 订单明细表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 订单明细主键 |
| `order_id` | `bigint` | 否 | 无 | 订单 ID |
| `product_id` | `bigint` | 否 | 无 | 商品 ID |
| `product_name` | `varchar(100)` | 否 | 无 | 下单时的商品名称快照 |
| `product_price` | `decimal(10,2)` | 否 | 无 | 下单时的商品单价快照 |
| `quantity` | `int` | 否 | `1` | 购买数量 |
| `subtotal` | `decimal(10,2)` | 否 | 无 | 明细小计 |
| `product_picture` | `varchar(255)` | 是 | `NULL` | 下单时的商品图片快照 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `idx_order_item_order` | 普通索引 | `order_id` | 查询一个或一批订单的明细 |
| `idx_order_item_product` | 普通索引 | `product_id` | 按商品反查订单明细 |

### 外键

无。`order_id` 和 `product_id` 都只是代码层逻辑引用。

## 10. `rider` 骑手表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 骑手主键 |
| `name` | `varchar(255)` | 否 | 无 | 骑手登录名称；注册 API 最长 50 个字符 |
| `phone` | `varchar(255)` | 否 | 无 | 骑手手机号 |
| `password` | `varchar(255)` | 否 | 无 | BCrypt 密码哈希 |
| `status` | `int` | 否 | 无 | `1` 正常、`0` 禁用 |
| `create_time` | `datetime` | 否 | 无 | 创建时间，由 MyBatis-Plus 自动填充 |
| `update_time` | `datetime` | 是 | `NULL` | 更新时间，由 MyBatis-Plus 在更新时填充 |
| `is_delete` | `int` | 否 | 无 | 逻辑删除标记：`0` 未删除、`1` 已删除 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_rider_name` | 唯一索引 | `name` | 保证骑手登录名称唯一 |
| `uk_rider_phone` | 唯一索引 | `phone` | 保证骑手手机号唯一 |

### 外键

无。

## 11. `delivery_task` 配送任务表

### 字段

| 字段 | 类型 | 允许 NULL | 默认值/附加属性 | 说明 |
|---|---|---:|---|---|
| `id` | `bigint` | 否 | `AUTO_INCREMENT` | 配送任务主键 |
| `order_id` | `bigint` | 否 | 无 | 订单 ID；唯一索引保证一单最多一个任务 |
| `rider_id` | `bigint` | 是 | `NULL` | 接单骑手 ID；待抢单时为空 |
| `merchant_name` | `varchar(255)` | 否 | 无 | 商家名称快照 |
| `delivery_reward` | `decimal(10,2)` | 否 | 无 | 配送奖励快照；当前固定为 `5.00` |
| `status` | `int` | 否 | 无 | `0` 待接单、`1` 配送中、`2` 已完成 |
| `create_time` | `datetime` | 否 | 无 | 商家出餐并创建任务的时间 |
| `accepted_time` | `datetime` | 是 | `NULL` | 骑手接单时间 |
| `delivered_time` | `datetime` | 是 | `NULL` | 骑手确认送达时间 |
| `update_time` | `datetime` | 是 | `NULL` | 更新时间 |
| `receiver_phone` | `varchar(20)` | 否 | 无 | 收货人电话快照 |
| `receiver_address` | `varchar(255)` | 否 | 无 | 收货地址快照 |
| `merchant_address` | `varchar(255)` | 否 | 无 | 商家地址快照 |
| `merchant_phone` | `varchar(20)` | 否 | 无 | 商家联系电话快照 |
| `receiver_name` | `varchar(20)` | 否 | 无 | 收货人姓名快照 |

### 索引

| 索引名 | 类型 | 字段 | 用途 |
|---|---|---|---|
| `PRIMARY` | 主键 | `id` | 主键定位 |
| `uk_delivery_task_order_id` | 唯一索引 | `order_id` | 保证一张订单最多创建一个配送任务，并为重复出餐兜底 |

### 外键

无。`order_id` 与 `rider_id` 目前仅由业务事务和条件更新维护。

## 12. 当前索引汇总

| 表 | 主键 | 唯一索引 | 普通索引 | 外键 |
|---|---:|---:|---:|---:|
| `user` | 1 | 2 | 0 | 0 |
| `merchant` | 1 | 2 | 0 | 0 |
| `category` | 1 | 1 | 1 | 0 |
| `product` | 1 | 1 | 2 | 1 |
| `cart` | 1 | 1 | 2 | 0 |
| `orders` | 1 | 2 | 2 | 0 |
| `order_item` | 1 | 0 | 2 | 0 |
| `rider` | 1 | 2 | 0 | 0 |
| `delivery_task` | 1 | 1 | 0 | 0 |
| **合计** | **9** | **12** | **9** | **1** |
